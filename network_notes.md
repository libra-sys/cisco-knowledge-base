计算机网络笔记

- 计算机网络基础     what why how   扩展知识

1、什么是数字 数据 信息 信号：

号传输数据，通信实



现信息传递

- 计算机网络基础：[郑]

- 什么是数字 数据 信息 信号：

数字是基础符号，数据是原始记录，信息是有用的数据，信号是传输的载体，通讯是信息传递的过程。

- 计算机网络是什么：

指将地理位置不同的具有独立功能的多台计算机及其外部设备，通过通信线路连接起来，是计算机的集合

- 计算机网络的功能有什么：

资源共享、数据通信、均衡负载和分布式处理、提高可靠性、集中处理，

- 计算机网络发展阶段有哪些：

第一阶段：面向终端的计算机网络通信

第二阶段：计算机与计算机互联

第三阶段：开放式标准化网络 网络与网络互联

第四阶段：高速互联与商业化网络

第五阶段：只能万物互联网络 物联网

- 计算机网络怎么分类：

地理位置划分：局域网(LAN)、城域网(MAN)、广域网(WAN)

传输介质划分：

有线连接：双绞线、同轴电缆、光纤

无线连接：微波、红外、卫星通讯

拓扑结构划分：

总线型、星形、环形、树状型、网状型

交换方式划分：

电路交换、分组交换、报文交换



- 计算机网络协议是什么：

协议：通讯双方共同遵守的规则约定

ISO国际标准化组织：OSI开放互联模型协议（官方）

OSI开放互联模型七层协议：

物理层 数据链路层 网络层 传输层 会话层 表示层 应用层

- 现在用的计算机网络协议 与OSI开放互联模型七层协议有什么区别：

各企业将七层协议改变为四层协议典型代表为TCP协议和IP协议

整个家族统称为TCP/IP协议           多数用于：局域网 广域网



七层模型对应的四层模型：

物理层、数据链路层：网络接口层

网络层：网际互连层协议：IP、ARP、IGMP、IGMP

传输层：传输层协议：TCP、UDP

会话层、表示层、应用层：应用层  协议：DNS、FTP、HTTP、HTTPS



- 每一层对应的协议有哪些：

应用层：

HTTP/HTTPS：超文本传输协议以及它的拓展版本(Hypertext Transfer Protocol)是一个简单的请求-响应协议

DNS：域名系统将域名转换成IP地址

FTP：文件传输协议，用于在网络上进行文件上传和下载

SMTP：简单邮件传输协议，主要用于电子邮件的发送或在邮件服务器之间进行中继

Telnet：远程登入协议，用于远程登入到网络设备或服务器，进行命令行管理

SNMP：简单网络管理协议，用于收集和管理网络设备(如路由器、交换机、打印机)的状态信息



传输层：

TCP：传输控制协议，一种面向连接的、可靠的协议，通过三次握手建立连接，会确认数据包是否到达，对丢失的数据包进行重传，并保证数据按顺序到达。因此，适用于对数据完整性要求高的场景，如文件传输、电子邮件和Web浏览



UDP：用户数据报协议，是一种无连接、不可靠的协议，它不建立连接、也不保证数据包一定能到达目的地，但优点是开销小、传输速度快，适用于对实时性要求高允许少量丢失的场景，比如语音视频通话、在线直播和DNS查询

网络层：

IP：网际协议，网络层的核心协议，它规定了数据包的格式和寻址方案，它的主要任务是根据目的IP地址，为数据包选择一条到达目标主机的路径，IP协议本身不可靠，它只尽力传输，不保证送达

ICMP：互联网控制报文协议：它主要用于在IP网络中传递错误报告和网络诊断信息

ARP：地址解析协议，功能是通过已知IP地址，解析出同一物理网络上对应的硬件地址(MAC地址)

RARP：反向地址解析，MAC地址 → IP地址

网络接口层：

以太网协议：这是目前最普遍的局域网技术标准，它定义了如何在网络介质上传输数据帧，以及如何通过MAC地址在直接相连的设备之间进行通信

PPP：点对点协议，常用于在两个网络节点之间建立直接的连接，比如早期的拨号上网或者现在的一些宽带连接技术



交换机配置命令：

enable进系统状态

conf t进全局配置

hostname修改主机名称

write保存

reload重启

description [注释]给端口添加注释

interface fa0/1配置交换机的1号端口 进入端口

speed设置端口传输速率

shutdown关闭端口

no shutdown开启端口

show running-config查看运行配置



以太网标准和以太网组网的基本方法：   [兰]



局域网标准： 

局域网标准主要是指由IEEE(美国电气电子工程师学会)制定的IEEE802系列标准

标准                                     说明 

IE802.1                       概述、体系结构、网络管理、网络互联 

IEEE802.2                           逻辑链路控制(LLC） 

EEE802.3(以太网)                CSMA/CD 访问方法、物理层规范 

IEEE802.4                           Token Bus(令牌总线) 

IEEE802.5                     Token Ring(令牌环)访间方法、物理层规范  

IEEE802.11 

(lEE80211a、IEEE802b、 

IEEE802.11g、 IEEE802.1ln)        无线局域网访问方法、物理层规范

IEEE802.12                  100VG- AnyLAN快速局域网访问方法、物理层规范 



以太网的产生：

以太网是应用最为广泛的局域网，指的是由 Xerox公司创建并由 Xerox、 Intel和DEC公 司联合开发的基带局域网规范，是当今现有局域网采用的最通用的通信协议标准。 符合IEEE802.3标准的局域网称为“以太网”。 

以太网组网方法：

采用CSMA/CD访问控制法

VLAN：

虚拟局域网是指网络中的站点不拘泥于所处的物理位置，而可以根据需要灵活地加入不 同的逻辑子网中的一种网络技术 IEEE 于1999 年颁布了用以标准化VLAN实现方案的EE802.1Q协议标准草案。VLAN 是为解决以太网的广播问题和安全性而提出的一种协议，它在以太网帧的基础上增加了 VLAN 头，用 VLAN ID把用户划分为更小的工作组，限制不同工作组间的用户互访，每个工 作组就是一个虚拟局域网。

划分虚拟局域网的依据：

1.基于网络性能  2.基于安全性  3.基于组织结构

采用虚拟局域网的优势：

1.增加网络的安全情况      2.集中化的管理控制

基于交换式以太网实现虚拟局域网的途径：

- 基于端口的虚拟局域网   2.基于IP地址的虚拟局域网

- 常用网络命令（ping,ipconfig）

- ping:

功能：测试计算机名和计算机的IP地址，验证与对方计算机的连接状态，通过向对方主 机发送“网际控制报文协议（ICMP)”回响请求消息来验证与对方TCP/IP计算机的IP级连 接，是用于检测网络连接性、可到达性和名称解析的疑难问题的主要TCP/IP命令。 

- t:ping 一个主机时系统不停地运行ping这个命令，直到按下Ctrl+C组合键。

- a:解析主机的NetBIOS主机名，一般是在运用ping命令后的第一行就显示出来

- n count:定义用来测试所发出的测试包（ECHO)的个数，默认值为4。通过这个命令可以 自己定义发送的测试包个数，对衡量网络速度很有帮助，例如想测试发送20个数据包的返 回平均时间为多少、最快时间为多少、最慢时间为多少，就可以通过执行带有这个参数的命令。 

- ipconfig:

1.Dipconfig/all:显示本机TCP/IP 配置的详细信息。为DNS和WINS服务器显示其已配置且 所要使用的附加信息（如IP地址、网关等）,并且显示内置于本地网卡中的物理地址(MAC)。 如果IP地址是从DHCP服务器租用的，ipconfig将显示DHCP服务器的IP地址和租用地址 预计失效的日期 2.ipconfig/release:DHCP 客户端手工释放IP地址，所有接口的租用IP地址便重新交付作 给DHCP服务器（归还IP地址）。该命令只能在向DHCP服务器租用其1P地址的计算机上起 作用。

3.ipconfig/renew:DHCP 客户端手工向服务器刷新请求，本地计算机便设法与DHCP服务器 取得联系，并租用一个IP地址。大多数情况下，网卡将被重新赋予和以前所赋予的相同的 IP 地址。

4.ipconfig/flushdns:可以进行 DNS 缓存刷新。



二进制转十进制

使用“按权相加法”，将二进制数的每一位乘以2的相应次幂，然后将所有乘积相加。

- 计算规则：从二进制数的最右边一位（最低位）开始，位置编号从0开始。每一位的权值为 2^位置编号。

- 示例：将二进制数 1011.01 转换为十进制。

- 整数部分 1011：1×2³ + 0×2² + 1×2¹ + 1×2⁰ = 8 + 0 + 2 + 1 = 11

- 小数部分 .01：0×2⁻¹ + 1×2⁻² = 0 + 0.25 = 0.25

- 合并结果：11 + 0.25 = 11.25

- 所以，二进制数 1011.01 对应的十进制数是 11.25。

十六进制转十进制

同样使用“按权相加法”，将十六进制数的每一位乘以16的相应次幂，然后将所有乘积相加。

- 计算规则：从十六进制数的最右边一位（最低位）开始，位置编号从0开始。每一位的权值为 16^位置编号。需要注意的是，十六进制中的字母A-F分别代表十进制的10-15。

- 示例：将十六进制数 38A.11 转换为十进制。

- 整数部分 38A：3×16² + 8×16¹ + 10×16⁰ = 3×256 + 8×16 + 10×1 = 768 + 128 + 10 = 906

- 小数部分 .11：1×16⁻¹ + 1×16⁻² = 0.0625 + 0.00390625 = 0.06640625

- 合并结果：906 + 0.06640625 ≈ 906.0664

- 所以，十六进制数 38A.11 对应的十进制数约为 906.0664。

这就将网络通信的全过程进行了深度整合。我们将数据传输看作一个完整的生命周期，把 OSI七层模型、UTF-8编码/解码、封装/解封装 以及各层的关键控制信息（关键字段）全部串联起来。

整个过程就像寄送一个特快专递：

- 内容准备：写信并翻译成通用语言（UTF-8）。

- 层层打包：写好收件人/发件人（地址、端口），一层套一层装箱。

- 物理运输：通过公路/铁路（物理介质）运送。

- 层层拆包：收件人一层层拆开，核对信息。

- 内容还原：翻译回原文。

以下是完整的整合流程：

🚀 第一阶段：发送端封装（自上而下）

数据从应用层产生，逐层向下，每层添加头部（Header）。

7. 应用层 -> 6. 表示层（内容编码阶段）

- 动作：UTF-8 编码

- 过程：用户输入文本（如 "你好"）。应用层将数据交给表示层。

- 关键字段/操作：字符集编码。

- 表示层将 Unicode 字符 "你好" 转换为 UTF-8 字节流：E4 BD A0 E5 A5 BD（十六进制）。

- 此时数据变为二进制，但还没有网络寻址信息。

6. 表示层 -> 5. 会话层（对话控制）

- 动作：建立会话连接

- 关键字段：会话ID (Session ID)、RPC (远程过程调用)

- 过程：标记这是哪一次具体的“对话”。如果是Web会话，可能会包含Cookie或Session标识。

5. 会话层 -> 4. 传输层（端到端传输）

- 动作：数据分段，添加传输层头部

- 关键字段：

- 源端口 (Source Port)：例如 50233（随机生成的客户端端口）

- 目标端口 (Destination Port)：例如 80 (HTTP) 或 443 (HTTPS)

- 序列号 (Sequence Number)：用于TCP重组

- 数据形态变化：此时数据被称为 段 (Segment)。

4. 传输层 -> 3. 网络层（路由寻址）

- 动作：添加IP头部

- 关键字段：

- 源IP地址 (Source IP)：例如 192.168.1.100

- 目标IP地址 (Destination IP)：例如 203.0.113.45

- TTL (生存时间)：防止数据包在网络中无限循环

- 数据形态变化：此时数据被称为 包 (Packet)。

3. 网络层 -> 2. 数据链路层（物理链路传输）

- 动作：添加帧头和帧尾

- 关键字段：

- 源MAC地址 (Source MAC)：例如 AA:BB:CC:DD:EE:FF

- 目标MAC地址 (Destination MAC)：例如 路由器的MAC地址

- FCS (帧校验序列)：用于检测物理传输中的错误

- 数据形态变化：此时数据被称为 帧 (Frame)。

2. 数据链路层 -> 1. 物理层（比特流传输）

- 动作：转换为物理信号

- 关键字段：无（此时只有0和1）

- 过程：将帧转换为电信号（网线）、光信号（光纤）或无线电波（Wi-Fi）。

- 数据形态变化：此时数据是 比特流 (Bits)。



🌐 第二阶段：传输介质

- 动作：物理传输

- 描述：比特流通过物理世界（网线、光纤、空气）从发送方传输到接收方。中间可能经过交换机（看MAC地址）、路由器（看IP地址）等设备。



📥 第三阶段：接收端解封装（自下而上）

数据到达接收方，逐层向上，每层移除对应的头部。

1. 物理层 -> 2. 数据链路层（信号转数据）

- 动作：接收电信号，转换为二进制帧。

- 关键检查：检查FCS帧校验序列。如果出错，直接丢弃（物理层不管重传）。

2. 数据链路层 -> 3. 网络层（检查物理地址）

- 动作：剥离帧头帧尾。

- 关键检查：检查目标MAC地址是否是自己。如果是，继续向上；如果不是，丢弃。

- 数据还原：取出内部的 包 (Packet)。

3. 网络层 -> 4. 传输层（检查逻辑地址）

- 动作：剥离IP头部。

- 关键检查：检查目标IP地址是否是自己，检查TTL是否过期。

- 数据还原：取出内部的 段 (Segment)。

4. 传输层 -> 5. 会话层（端口分发）

- 动作：剥离传输层头部。

- 关键检查：根据目标端口（如80端口），将数据交给正在监听该端口的服务器进程（如Nginx/Apache）。

- 可靠性处理：如果是TCP，进行确认应答(ACK)和数据重组。

5. 会话层 -> 6. 表示层（内容解码）

- 动作：UTF-8 解码

- 关键字段/操作：字符集解码

- 接收到来自下层的字节流 E4 BD A0 E5 A5 BD。

- 表示层识别出这是UTF-8编码，将其转换回 Unicode 字符 "你好"。

6. 表示层 -> 7. 应用层（最终呈现）

- 动作：数据交给应用程序。

- 结果：Web服务器或聊天软件收到原始的文本数据 "你好"，并在屏幕上显示出来。

📌 核心总结表（关键字段一览）

这个流程展示了数据如何从“人类可读的文字”变成“物理信号”穿越网络，再变回“人类可读的文字”。

这就将网络通信的微观细节进行深度整合。为了让你看清 UTF-8 编码 与 二进制封装 是如何融合在一起的，我们从“字符”一直看到“物理信号”，进行一次彻底的拆解。

核心结论是：UTF-8 编码后的二进制，就是整个数据包的“有效载荷”（Payload）。



🧬 第一层：字符变二进制（表示层）

场景：你在浏览器输入 Hi 并发送。

- UTF-8 编码过程：

- H 的 ASCII/UTF-8 码是 72，二进制为 01001000

- i 的 ASCII/UTF-8 码是 105，二进制为 01101001

- 结果：字符串 Hi 变成了 16 位的二进制流：01001000 01101001

关键点：此时这串二进制只是“原始数据”，还没有任何网络传输的指令。



📦 第二层：二进制的层层封装（数据结构化）

这串 01001000 01101001 开始向下封装，每一层都在它前面（有时在后面）加上“控制信息”。

1. 传输层封装（加“快递单第一行”）

- 动作：TCP 头部 + 数据

- 结构：

- 头部：源端口(2字节) + 目标端口(2字节) + 序列号(4字节) + ...

- 数据：01001000 01101001

- 结果：这一整块数据被称为 段 (Segment)。

2. 网络层封装（加“快递单第二行”）

- 动作：IP 头部 + 传输层段

- 结构：

- 头部：源IP(4字节) + 目标IP(4字节) + TTL(1字节) + ...

- 数据：[TCP头部 + 01001000 01101001]

- 结果：这一整块数据被称为 包 (Packet)。

3. 数据链路层封装（加“快递单第三行”和“封条”）

- 动作：帧头 + 网络层包 + 帧尾

- 结构：

- 头部：源MAC(6字节) + 目标MAC(6字节) + 类型(2字节)

- 数据：[IP头部 + [TCP头部 + 01001000 01101001]]

- 尾部：FCS 校验码 (4字节)

- 结果：这一整块数据被称为 帧 (Frame)。



⚡ 第三层：二进制变信号（物理层）

此时，整个 帧 (Frame) 是一个巨大的二进制串。它由两部分组成：

- 控制部分：最前面的 MAC 地址、IP 地址、端口号等信息。

- 数据部分：深处的 01001000 01101001 (即原始的 UTF-8 编码数据)。

转换过程（数模转换）： 物理层（网卡/调制解调器）不管数据是什么含义，它只负责把这串巨大的 0 和 1 转换成物理信号。

- 网线 (以太网)：

- 0 = 低电压 (例如 0V)

- 1 = 高电压 (例如 5V)

- 信号形态：电信号

- 光纤：

- 0 = 无光脉冲

- 1 = 有光脉冲

- 信号形态：光信号

- Wi-Fi：

- 0/1 = 不同频率或相位的无线电波。

- 信号形态：电磁波



整合图解（数据透视）

假设你要传输字符 A，我们看它在整个链条中的形态变化：

总结： UTF-8 编码决定了原始数据变成什么样的二进制；封装决定了这些二进制被包裹在什么样的“信封”里；物理层决定了这个信封如何通过物理世界传输。

这就将共享介质、总线结构、信道冲突以及冲突域/广播域这几个紧密相关的概念整合进来。它们是理解局域网底层原理（特别是以太网进化史）的关键。

我们可以把局域网看作一个“办公室”，这几个概念分别描述了办公室的“工位布局”、“沟通方式”和“管理规则”。



1. 共享介质与总线结构：最初的“大通铺”

在早期的局域网（如经典的10BASE5粗缆以太网）中，核心思想是共享介质。

- 总线型拓扑 (Bus Topology)：

- 结构：所有计算机都连接到一根长长的同轴电缆（这根电缆就是总线）上。

- 本质：这根总线就是共享介质。同一时间，只能有一台电脑发消息，其他电脑都得“竖着耳朵”听。

- 问题：这就像是办公室里所有人共用一张大桌子（共享介质）和一个大喇叭（总线）。如果两个人同时说话，声音就会混在一起，谁也听不清（冲突）。

2. 信道冲突与 CSMA/CD：最初的“说话规则”

在共享介质环境下，信道冲突是不可避免的物理现象。

- 冲突 (Collision)：

- 当两台或多台计算机同时检测到线路空闲并开始发送数据时，电信号在总线（共享介质）上会发生碰撞，导致数据损坏。

- CSMA/CD (载波侦听多路访问/冲突检测)：

- 为了解决冲突，早期以太网制定了这套规则：

- 先听后发 (Carrier Sense)：说话前先听一下有没有人正在说话。

- 边发边听 (Collision Detect)：说话的同时还要听着，如果听到“嗡嗡”的杂音（冲突），说明有人跟你同时说话了。

- 冲突停发：一旦检测到冲突，立刻停止说话，并大喊一声“冲突了！”（阻塞信号），然后随机等待一段时间再尝试。

3. 冲突域 (Collision Domain)：冲突发生的范围

冲突域是网络中一个非常重要的物理概念，它定义了冲突的影响范围。

- 定义：

- 网络中所有共享同一传输介质、并且可能发生数据冲突的设备集合。

- 换句话说，就是在这个区域内，如果一台设备发数据，其他设备都必须监听，且可能发生撞车。

- 设备影响：

- 集线器 (Hub)：是一个典型的物理层设备，它没有任何智能，只是简单地把收到的电信号放大并转发给所有其他端口。因此，连接在同一个集线器上的所有设备，都处于同一个冲突域中。

- 交换机 (Switch)：是一个数据链路层设备。它的每个端口都是一个独立的冲突域。因为交换机为每个端口提供了专用的带宽，设备可以直接点对点通信，不再需要“争抢”总线。交换机的出现，从根本上隔离了冲突域，让每个端口变成了一个独立的“私有通道”，不再发生冲突。

4. 广播域 (Broadcast Domain)：广播消息的范围

如果说冲突域是解决“谁先说话”的问题，那么广播域就是解决“谁来听这个大喇叭”的问题。

- 定义：

- 网络中所有能接收到同样广播消息的设备集合。

- 广播是一种“一对所有”的通信方式（例如：主机刚上线时问“IP地址为X.X.X.X的MAC地址是谁？”）。

- 设备影响：

- 交换机 (Switch)：不能隔离广播域。交换机收到广播帧（目标MAC为 FF:FF:FF:FF:FF:FF）后，会把它转发到除接收端口外的所有端口。所以，默认情况下，连接在同一台交换机（或互连的多台交换机）上的所有设备，都处于同一个广播域中。

- 路由器 (Router)：是网络层设备，它是隔离广播域的天然屏障。路由器不会转发二层的广播包，它会把广播限制在接口所在的子网内。



📌 终极对比：冲突域 vs 广播域

📝 总结你的局域网视图

- 最初：大家挤在一根总线（共享介质）上，用 CSMA/CD 规则抢着说话，整个网络既是一个冲突域，也是一个广播域。

- 现在：我们用交换机把大家连起来。

- 交换机把冲突域切碎了，每人一个独立房间（端口），互不干扰，不需要 CSMA/CD（全双工模式）。

- 但大家依然在一个广播域里，如果有人广播找人，所有人（同一网段）还是能收到这个广播。如果要隔绝广播，必须用路由器或VLAN。

交换技术是计算机网络和通信系统中实现数据高效转发与传输的核心机制，通过在源节点与目标节点之间建立动态连接路径，确保信息准确、快速地到达目的地，其发展与演进构成了现代网络通信的基础架构。

一、交换技术基础概念与原理

1. 核心定义

交换技术是指在网络通信中，通过交换设备（如交换机、路由器等）实现数据从源节点到目标节点的动态转发过程。其本质是解决多用户通信需求，通过构建网络连接，使任意用户间能够进行信息交互。

2. 基本工作原理

交换技术的核心原理是建立连接→数据传输→拆除连接的三阶段过程：

- 连接建立：源节点发起通信请求，网络中的交换设备协同工作，寻找并建立一条从源到目的的通信路径

- 数据传输：数据通过建立的路径进行传输

- 连接拆除：通信结束后，释放占用的网络资源

二、主要交换技术类型及特点

1. 电路交换 (Circuit Switching)

核心原理：在通信开始前建立一条专用的物理通信路径，该路径在整个通信期间由双方独占，直到通信结束才释放。

工作流程：

- 电路建立：通过源站点请求完成交换网中对应节点的接续过程

- 数据传输：信号从源站经建立的链路传送到目的站

- 电路拆除：完成传输后，由源站或目的站提出终止通信，各节点拆除连接

优点：

- 通信时延极小，数据传输稳定可靠

- 有序传输，分组到达顺序与发送顺序一致

- 控制简单，交换节点逻辑相对简单

缺点：

- 建立连接开销大，呼叫建立时间长

- 线路利用率低，即使在通信静默期，资源也无法被其他用户使用

- 灵活性差，无法动态绕过故障点

典型应用：传统电话网络(PSTN)、实时音视频会议、铁路/电力专线通信

2. 报文交换 (Message Switching)

核心原理：采用"存储-转发"机制，数据被封装成完整报文，每个报文包含源地址、目的地址和数据本身，报文被整个发送到下一个节点，该节点完整接收并存储后，再根据目的地址选择合适路径转发。

优点：

- 无需建立连接，省去建立和释放连接的开销

- 线路利用率高，多个用户的报文可共享网络带宽

- 支持多目标和差错校验，提高传输可靠性

缺点：

- 存储转发时延高，尤其当报文很长时

- 对节点缓存要求高，需要大容量缓存来存储待转发报文

- 可靠性问题，中间节点故障可能导致报文丢失

典型应用：早期电报系统、电子邮件等对实时性要求不高的场景

3. 分组交换 (Packet Switching)

核心原理：将数据分割成小的数据包，每个数据包独立发送，通过网络根据目标地址进行路由选择，到达目的地后再按顺序重新组装。

优点：

- 资源利用率高，多个用户数据可共享网络带宽

- 灵活性好，适应不同速率和格式的数据传输

- 简化存储管理，分组长度固定

- 加速传输，通过并行处理和转发

缺点：

- 可能引入转发时延，网络拥塞时延可能显著增加

- 需要复杂的路由和管理机制

典型应用：互联网数据传输、网页浏览、文件下载、即时通讯

4. 局域网交换技术

4.1 端口交换

- 原理：基于OSI第一层物理层，通过调整模块或端口连接的网段实现负载平衡

- 特点：具有灵活性和负载平衡能力，但未改变共享介质特性

- 应用：早期应用于插槽式集线器的端口分配

4.2 帧交换

- 原理：通过对传统传输媒介进行微分段，提供并行传送机制，减小冲突域，提高带宽

- 处理方式：

- 直通交换：提供线速处理能力，只读出网络帧的前14个字节即转发

- 存储转发：对网络帧进行完整读取、验错和控制

- 特点：应用最广的局域网交换技术，支持高级控制功能

4.3 信元交换

- 原理：采用固定长度53字节的信元进行交换，易于硬件实现

- 特点：

- 采用专用的非差别连接，并行运行

- 可通过一个交换机同时建立多个节点，不影响节点间通信

- 支持建立多个虚拟链接，提高带宽和容错能力

- 带宽可达25M、155M、622M甚至数Gb

- 典型代表：ATM(异步传输模式)技术

三、交换技术的演进历程

交换技术发展经历了从简单到复杂、低效到高效的演进过程：

- 人工交换阶段：早期通过电话接线员手工操作完成电话接续

- 机电交换阶段：步进制、纵横制等机电交换设备出现

- 电子交换阶段：1965年首台存储程序控制的电子交换机问世

- 数字交换阶段：数字技术应用于交换系统

- 宽带交换阶段：ATM和IP为核心的宽带分组交换技术发展

- 光交换阶段：光交换技术突破电子速率瓶颈，实现太空数据传输"零误码"

现代交换技术正朝着智能化、全光网络方向演进，软交换技术实现控制与传输分离，光交换技术突破传统电子速率限制。

四、交换技术在现代网络中的应用

1. 企业网络

- 应用：连接企业内部服务器、工作站、打印机等终端设备

- 特点：提供VLAN划分、访问控制列表等高级功能，提高网络安全性

- 设备：二层/三层交换机，支持端口聚合、生成树协议等

2. 数据中心

- 应用：连接大量服务器和存储设备，处理海量数据传输

- 特点：提供高密度端口连接、高速数据传输、复杂网络拓扑

- 设备：高性能核心交换机，配备冗余电源和风扇，确保高可靠性

3. 工业物联网

- 应用：连接工厂、车间的传感器、控制器等智能设备

- 特点：需要工业交换机，具备宽温(-40℃~+85℃)、抗电磁干扰、冗余环网等特性

- 案例：汽车制造厂焊接车间部署工业交换机，组成ERPS环网，连接焊接机器人和传感器

4. 视频监控系统

- 应用：连接监控摄像头、存储设备和显示终端

- 特点：需要带宽计算、PoE供电、组播功能

- 配置：千兆PoE交换机，总PoE功率需大于所有摄像头功率之和

5. 公共场所WiFi覆盖

- 应用：机场、商场等场所的无线网络覆盖

- 特点：需要高密度接入、WiFi 6适配、负载均衡

- 配置：支持2.5G端口的交换机，背板带宽需足够大(如48口千兆交换机需96Gbps)

五、交换技术的选择与发展趋势

1. 交换技术选择依据

- 实时性要求：语音/视频通信适合电路交换，数据传输适合分组交换

- 资源利用率：突发性数据适合分组交换，持续性数据可考虑电路交换

- 网络规模：小型网络可使用二层交换，大型网络需三层交换

- 应用场景：工业环境需工业交换机，普通办公环境可使用商用交换机

2. 未来发展趋势

- 软件定义网络(SDN)：通过集中式控制器实现网络的灵活控制和动态调整

- 网络功能虚拟化(NFV)：将网络功能从专用硬件解耦，提高灵活性和降低成本

- 5G核心网切片：为不同应用场景提供定制化网络服务

- 全光网络：光交换技术将突破电子速率瓶颈，实现更高速率的数据传输

3. 交换技术的融合与创新

现代网络中，多种交换技术常常融合使用：

- 混合交换：根据数据流量和需求，灵活选择电路交换或分组交换

- 集成模型：如MPLS、IP交换等，将多种交换技术优势结合

- 多层交换：二层交换、三层交换和七层交换协同工作，满足复杂网络需求

六、总结与建议

交换技术作为网络通信的基石，其选择与应用直接影响网络性能和用户体验。在实际网络建设中，应根据应用场景、性能需求、成本预算等因素，合理选择交换技术类型和设备。

关键建议：

- 小型办公网络：可采用二层交换机，注重性价比和易用性

- 大型企业网络：需部署三层交换机，支持VLAN和路由功能

- 工业环境：必须使用工业交换机，确保宽温、抗干扰和冗余能力

- 高带宽需求场景：如视频监控、WiFi 6覆盖，需选择高背板带宽和适当端口速率的交换机

随着网络技术的不断发展，交换技术将继续演进，为更高效、更智能的网络通信提供坚实基础。理解各种交换技术的原理、特点和适用场景，是构建高性能网络的关键。

# show mactab



跨交换机VLAN划分是实现网络逻辑隔离和高效管理的关键技术，通过在多台交换机上创建相同的VLAN并配置Trunk链路，使同一VLAN的设备即使连接在不同交换机上也能通信，同时实现不同VLAN间的隔离。

一、跨交换机VLAN划分的核心原理

1. VLAN基础概念

VLAN（Virtual Local Area Network） 是一种将物理网络划分为多个逻辑子网的技术，主要作用包括：

- 隔离广播域：限制广播流量范围，提高网络性能

- 增强安全性：逻辑分组控制数据流动，保护敏感信息

- 简化管理：灵活的逻辑分组比物理布线更便于管理

2. 跨交换机VLAN的关键机制

- VLAN一致性：所有交换机上必须创建相同ID和名称的VLAN

- Trunk链路：交换机间连接的端口必须配置为Trunk模式，允许多个VLAN数据通过

- VLAN标签：Trunk链路使用802.1Q标准在数据帧上添加VLAN标签，实现逻辑信道复用

二、思科交换机跨交换机VLAN配置步骤

1. 基础配置流程

- 创建VLAN：在所有交换机上创建相同的VLAN

- 分配端口：将接入设备的端口分配到相应VLAN

- 配置Trunk：设置交换机间连接端口为Trunk模式

- 验证配置：检查VLAN和Trunk状态

2. 详细配置示例（两台交换机+四台PC）

拓扑结构

- 交换机1(SW1)：连接PC0(VLAN10)和PC1(VLAN20)

- 交换机2(SW2)：连接PC2(VLAN10)和PC3(VLAN20)

- Trunk链路：SW1的Fa0/24 ↔ SW2的Fa0/24

IP地址规划

交换机1(SW1)配置

SW1> enable

SW1# configure terminal

! 创建VLAN

SW1(config)# vlan 10

SW1(config-vlan)# name Sales

SW1(config-vlan)# exit

SW1(config)# vlan 20

SW1(config-vlan)# name Engineering

SW1(config-vlan)# exit

! 配置接入端口

SW1(config)# interface fastEthernet 0/1

SW1(config-if)# switchport mode access

SW1(config-if)# switchport access vlan 10

SW1(config-if)# no shutdown

SW1(config)# interface fastEthernet 0/2

SW1(config-if)# switchport mode access

SW1(config-if)# switchport access vlan 20

SW1(config-if)# no shutdown

! 配置Trunk端口

SW1(config)# interface fastEthernet 0/24

SW1(config-if)# switchport mode trunk

SW1(config-if)# switchport trunk allowed vlan 10,20

设卡可以在vlan10上行，vlan20拒绝sw t al vlan10

只可以vlan10安全通信

SW1(config-if)# no shutdown

! 保存配置

SW1(config)# end

SW1# copy running-config startup-config

交换机2(SW2)配置

SW2> enable

SW2# configure terminal

! 创建VLAN（必须与SW1一致）

SW2(config)# vlan 10

SW2(config-vlan)# name Sales

SW2(config-vlan)# exit

SW2(config)# vlan 20

SW2(config-vlan)# name Engineering

SW2(config-vlan)# exit

! 配置接入端口

SW2(config)# interface fastEthernet 0/1

SW2(config-if)# switchport mode access

SW2(config-if)# switchport access vlan 10

SW2(config-if)# no shutdown

SW2(config-if)# switchport mode access

SW2(config-if)# switchport access vlan 20

SW2(config-if)# no shutdown

! 配置Trunk端口

SW2(config)# interface fastEthernet 0/24

SW2(config-if)# switchport mode trunk

SW2(config-if)# switchport trunk allowed vlan 10,20

SW2(config-if)# no shutdown

! 保存配置

SW2(config)# end

SW2# copy running-config startup-config

三、关键配置要点与验证方法

1. Trunk配置关键点

- 端口模式：交换机间连接端口必须设置为trunk模式，而非默认的access模式

- 允许VLAN：使用switchport trunk allowed vlan命令指定允许通过的VLAN

- 本征VLAN：默认为VLAN 1，但建议显式设置并确保两端匹配，避免控制帧丢弃

2. 验证配置的常用命令

- 查看VLAN信息：

Switch# show vlan brief



- 检查Trunk状态：

Switch# show interfaces trunk



- 查看端口详细信息：

Switch# show interfaces gigabitethernet 0/1 switchport



- 测试连通性：

PC> ping 192.168.10.2



3. 常见问题排查

- 问题：同一VLAN设备无法跨交换机通信 解决：检查Trunk端口是否配置正确，确保两端都允许了相关VLAN

- 问题：不同VLAN设备意外通信 解决：检查端口是否被错误地分配到多个VLAN，或Trunk配置不当

- 问题：Trunk端口显示为down状态 解决：确认物理连接正常，检查两端端口配置是否匹配

四、高级应用场景与最佳实践

1. 多交换机环境下的VLAN管理

- VTP（VLAN Trunking Protocol）：用于在多台交换机间同步VLAN信息，减少配置工作量

- 分层设计：核心层、分布层和接入层交换机的VLAN规划策略

2. 安全性增强措施

- 禁用未使用的端口：减少攻击面

- 限制Trunk端口：只允许必要的VLAN通过

- 配置端口安全：防止MAC地址欺骗

3. 实际应用案例

- 企业网络：财务部(VLAN10)、技术部(VLAN20)、HR(VLAN30)的逻辑隔离

- 校园网络：教学区、办公区、宿舍区的网络分隔

- 数据中心：不同业务系统的网络隔离

4. 最佳实践建议

- 命名规范：为VLAN使用有意义的名称，便于管理

- 文档记录：维护VLAN分配和端口映射的详细文档

- 配置备份：定期备份交换机配置，便于故障恢复

- 测试验证：配置后务必进行连通性测试和隔离验证

五、总结与展望

跨交换机VLAN划分是网络工程中的基础但至关重要的技术，它使网络管理员能够在物理网络基础上构建灵活的逻辑网络结构。

组网采用等保2.0或是3.0

企业级路由器

正确配置的跨交换机VLAN不仅能提高网络性能和安全性，还能简化网络管理和故障排查。

随着网络技术的发展，VLAN技术也在不断演进：

- SDN（软件定义网络）：通过集中式控制器实现更灵活的VLAN管理

- VXLAN（虚拟扩展局域网）：解决传统VLAN数量限制问题

- 自动化配置：使用Ansible、Python等工具实现VLAN配置自动化

掌握跨交换机VLAN划分技术是网络工程师的基础能力，也是构建高效、安全、可管理网络的关键一步。通过合理规划和配置，VLAN技术能够有效支持现代企业的网络需求，为业务提供稳定可靠的网络基础。

- 拓展

- 华为防火墙，硬件防火墙，网络设备，区域隔离，防病毒，放攻击，上网流量管理

- UGS6000按需求部署，部署出口——中小型公司，（路由器）pppoe/dhcp/nat/dhcp服务器

- 安全隔离/放病毒---隐患--罢工

- 防火墙：安全防护，都是为了保护公司内部网络安全

- bandwidth 配置接口带宽——校园网路由器配置

- 机房网络-星形——局域网——分组交换



- IEEE802.1

- IEEE802.2

- IEEE802.11无线局域网

网络拓扑方案设计：推荐采用 “企业级路由器 + PoE交换机 + 无线AP” 的经典架构。这种方案稳定可靠，易于扩展。

- 不可以。在三层交换机中，不同 VLAN 的接口（SVI）不能配置同一个 IP 地址，也不能配置属于同一个网段的 IP 地址。

- 这是网络通信的基本规则，原因如下：

- 🚫 为什么不能是同一个 IP？

- IP 地址的唯一性

- 在同一个网络环境中，IP 地址必须是唯一的。如果 VLAN 10 和 VLAN 20 都配置了 192.168.1.1，交换机会立即报错，提示“IP 地址重叠”或“接口冲突”，因为它无法判断发往 192.168.1.1 的数据包到底应该交给哪个 VLAN 处理。

- 路由表无法建立

- 三层交换机依靠路由表来转发数据。当你为 VLAN 配置 IP 时，交换机会自动生成一条“直连路由”。

- 如果 VLAN 10 配了 192.168.10.1/24，路由表里就会有一条去往 192.168.10.0/24 网段的路。

- 如果 VLAN 20 也配 192.168.10.2/24（同网段），路由表就会混乱，它不知道该通过 VLAN 10 还是 VLAN 20 去转发数据，导致通信中断。

- 🛠️ 正确的配置方式（一一对应）

- 为了实现不同 VLAN 互通，每个 VLAN 必须拥有独立的网段，且网关 IP 必须唯一。

- 表格

- VLAN ID建议网段交换机接口 IP (网关)说明

- VLAN 10192.168.10.0/24192.168.10.1办公网网关

- VLAN 20192.168.20.0/24192.168.20.1访客网网关

- VLAN 30192.168.30.0/24192.168.30.1服务器网网关

- 💡 特殊情况：Super VLAN（VLAN 聚合）

- 你可能听说过“不同 VLAN 用同一个网段”，这通常是指一种高级技术叫 Super VLAN（或 VLAN 聚合）。

- 原理：它允许将多个子 VLAN（Sub-VLAN）归入一个 Super VLAN。

- IP 配置：只有 Super VLAN 配置 IP 地址（作为统一网关），子 VLAN 不配置 IP。

- 目的：这主要是为了节省 IP 地址资源，而不是为了让它们共享同一个 IP。

- 注意：这需要交换机支持该功能（如华为、华三的部分设备），且配置复杂，普通组网不推荐使用。

- 📌 总结

- 在常规的思科、华为、H3C 等三层交换机配置中：

- VLAN 10 的网关必须是 A 网段（如 192.168.1.1）。

- VLAN 20 的网关必须是 B 网段（如 192.168.2.1）。

- 绝对不能 两个 VLAN 接口配置相同的 IP，或者处于同一网段。

- 三层交换机

- ip routing

- ip route 

- sw m t

- sw t en d

- sw t allowed vlan all

以下是交换机地址表工作的核心原理：

1. 地址表的建立（自学习）

交换机上电时，地址表为空。当它从某个端口收到数据帧时，会执行以下操作：

- 提取源MAC地址：解析数据帧的源MAC地址。

- 记录对应关系：将该MAC地址与接收端口号、所属VLAN（如适用）关联，形成一个表项（如：MAC地址 → 端口X → VLAN Y）。

- 动态更新：如果该MAC地址已存在，交换机会刷新其老化时间戳，确保表项保持最新。

2. 数据帧的转发（查表匹配）

当交换机收到一个数据帧时，会根据目的MAC地址进行决策：

- 匹配成功：若地址表中存在该目的MAC地址，交换机直接将帧转发到对应的端口，实现点对点通信。

- 匹配失败：若地址表中无该目的MAC地址（或为广播/多播地址），交换机会将帧泛洪（Flooding）到除接收端口外的所有端口，确保目标设备能收到。

3. 老化机制

为适应网络拓扑变化，动态表项并非永久有效：

- 老化时间：每条动态表项有生存周期（默认通常为300秒），若在该时间内未收到对应MAC地址的帧，表项会被删除。

- 随机清理：老化过程可能在设定时间范围内随机触发，避免同时删除大量表项影响性能。

4. 特殊场景处理

- VLAN支持：地址表项包含VLAN ID，确保同一VLAN内的通信隔离不同VLAN。

- 特殊帧过滤：如BPDU（生成树协议报文）等特殊帧可能不参与地址学习，避免表项污染。

通过上述机制，交换机能够高效维护地址表，实现低延迟、高带宽的网络通信，同时隔离冲突域，提升网络稳定性。

- 网络位不变

- 主机位不变

- 子网2n-2

- 主机2n

- 广播全1

- A类私有地址

- 范围：10.0.0.0 至 10.255.255.255

- 网络前缀：10.0.0.0/8

- 特点：这是一个完整的A类网络，包含超过1600万个地址，通常用于大型企业或数据中心的内部网络。

- B类私有地址

- 范围：172.16.0.0 至 172.31.255.255

- 网络前缀：172.16.0.0/12

- 特点：这个范围包含了16个连续的B类网络，适用于中型企业或特定应用场景（如Docker默认网络）。

- C类私有地址

- 范围：192.168.0.0 至 192.168.255.255

- 网络前缀：192.168.0.0/16

- 特点：这个范围包含了256个C类网络，每个网络有254个可用地址，是目前最常见、最广泛用于家庭和小型办公室网络的私有地址段。



- 快速安全稳定可靠

结合之前的拓扑图和生成树协议的原理，这里为你整理一份详细的思科 Packet Tracer 命令配置笔记。

这份笔记将分为生成树协议和链路聚合两个部分，重点在于解释每条命令的作用以及为什么要这样配置。



思科配置笔记：生成树协议与链路聚合

一、生成树协议

目标： 在存在物理环路的网络中，通过逻辑阻塞端口来防止广播风暴。

核心命令逻辑：

启用生成树协议： 思科交换机默认通常已启用 PVST+，但显式开启可以确保配置生效。

配置根桥优先级： 手动指定哪台交换机成为根桥。

优化接入端口： 防止接入 PC 的端口触发生成树计算。

查看状态： 验证配置是否生效。

配置示例（以拓扑图为例）：

假设我们将 交换机3 配置为根桥，交换机1 和 交换机2 为非根桥。

1. 交换机3 (Switch3) - 根桥配置

Switch> enable

Switch# configure terminal

Switch(config)# hostname Switch3



! 1. 启用生成树协议 (默认已开，显式配置更稳妥)

Switch3(config)# spanning-tree mode pvst



! 2. 配置根桥优先级 (默认32768，数字越小优先级越高)

Switch3(config)# spanning-tree vlan 1 priority 4096



! 3. 配置连接 PC 的接入端口

Switch3(config)# interface FastEthernet0/3

Switch3(config-if)# switchport mode access

Switch3(config-if)# switchport access vlan 1

Switch3(config-if)# spanning-tree portfast    ! 启用 PortFast，跳过监听/学习状态

Switch3(config-if)# no shutdown

Switch3(config-if)# exit



! 4. 配置中继端口 (连接其他交换机)

Switch3(config)# interface range FastEthernet0/1 - 2

Switch3(config-if-range)# switchport mode trunk

Switch3(config-if-range)# no shutdown

Switch3(config-if-range)# exit



! 5. 保存配置

Switch3# copy running-config startup-config

2. 交换机1 (Switch1) - 非根桥配置

Switch> enable

Switch# configure terminal

Switch(config)# hostname Switch1



! 1. 启用生成树协议

Switch1(config)# spanning-tree mode pvst



! 2. 配置连接 PC 的接入端口

Switch1(config)# interface FastEthernet0/1

Switch1(config-if)# switchport mode access

Switch1(config-if)# switchport access vlan 1

Switch1(config-if)# spanning-tree portfast

Switch1(config-if)# no shutdown

Switch1(config-if)# exit



! 3. 配置中继端口

Switch1(config)# interface range FastEthernet0/2 - 3

Switch1(config-if-range)# switchport mode trunk

Switch1(config-if-range)# no shutdown

Switch1(config-if-range)# exit



! 4. 保存配置

Switch1# copy running-config startup-config

3. 交换机2 (Switch2) - 非根桥配置

Switch> enable

Switch# configure terminal

Switch(config)# hostname Switch2



! 1. 启用生成树协议

Switch2(config)# spanning-tree mode pvst



! 2. 配置中继端口

Switch2(config)# interface range FastEthernet0/1 - 2

Switch2(config-if-range)# switchport mode trunk

Switch2(config-if-range)# no shutdown

Switch2(config-if-range)# exit



! 3. 保存配置

Switch2# copy running-config startup-config

验证命令：

! 查看生成树摘要信息 (最常用)

Switch# show spanning-tree summary



! 查看特定 VLAN 的详细生成树状态

Switch# show spanning-tree vlan 1



! 查看端口角色和状态

Switch# show spanning-tree interface



二、链路聚合

目标： 将多条物理链路捆绑成一条逻辑链路，增加带宽并提供冗余。

核心命令逻辑：

创建端口信道： 创建一个逻辑接口（Port-Channel）。

配置模式： 选择手动模式或动态协商模式。

将物理端口加入信道： 将需要捆绑的物理端口分配给逻辑接口。

配置示例：

假设我们将 交换机1 和 交换机2 之间的两条链路（Fa0/2 和 Fa0/3）进行聚合。

1. 交换机1 (Switch1) - 链路聚合配置

Switch1> enable

Switch1# configure terminal



! 1. 创建端口信道并进入其配置模式

Switch1(config)# interface Port-channel 1



! 2. 配置端口信道为中继模式

Switch1(config-if)# switchport mode trunk

Switch1(config-if)# exit



! 3. 进入物理端口范围

Switch1(config)# interface range FastEthernet0/2 - 3



! 4. 将物理端口加入端口信道 (channel-group)，模式为 on (手动)

Switch1(config-if-range)# channel-group 1 mode on



! 5. 设置物理端口为中继模式

Switch1(config-if-range)# switchport mode trunk

Switch1(config-if-range)# no shutdown

Switch1(config-if-range)# exit



! 6. 保存配置

Switch1# copy running-config startup-config

2. 交换机2 (Switch2) - 链路聚合配置

Switch2> enable

Switch2# configure terminal



! 1. 创建端口信道并进入其配置模式

Switch2(config)# interface Port-channel 1



! 2. 配置端口信道为中继模式

Switch2(config-if)# switchport mode trunk

Switch2(config-if)# exit



! 3. 进入物理端口范围

Switch2(config)# interface range FastEthernet0/1 - 2



! 4. 将物理端口加入端口信道 (channel-group)，模式为 on (手动)

Switch2(config-if-range)# channel-group 1 mode on



! 5. 设置物理端口为中继模式

Switch2(config-if-range)# switchport mode trunk

Switch2(config-if-range)# no shutdown

Switch2(config-if-range)# exit



! 6. 保存配置

Switch2# copy running-config startup-config

验证命令：

! 查看端口信道摘要信息

Switch# show etherchannel summary



! 查看端口信道的详细状态

Switch# show etherchannel port-channel



总结

- 生成树协议是通过阻塞端口来防止环路，牺牲了部分带宽。

- 链路聚合是通过捆绑端口来消除环路并增加带宽。

- 在实际网络中，通常会结合使用这两种技术，以实现高带宽和高冗余的网络环境。

- 静态路由



- 业务ip 给客户

- 互联ip 路由器互联

- 管理ip 交换机或路由器原程管理 

- 同一个网段即同一个广播域

- 浮动静态路由

- ip route 目的地址 子网掩码 下一跳 AD

- 动态路由：

- 1. RIP (Routing Information Protocol)

- RIP 使用最简单直接的度量方式。

- 度量参数：跳数 (Hop Count)

- 工作原理：数据包从源到目的地需要经过的路由器数量。直连网络为0跳，经过一个路由器为1跳，以此类推。

- 限制：RIP 规定最大跳数为15，16跳即被视为不可达，这限制了其在大型网络中的应用。

- 2. OSPF (Open Shortest Path First)

- OSPF 的度量方式更能反映链路的实际性能。

- 度量参数：开销 (Cost)

- 工作原理：开销与链路带宽成反比。带宽越高，开销越低，路径越优先。

- 计算公式：Cost = 参考带宽 / 接口带宽

- 参考带宽 (Reference Bandwidth)：一个预设值，默认为 100 Mbps。

- 接口带宽 (Interface Bandwidth)：路由器出接口的实际带宽。

- 示例：一条 100 Mbps 链路的开销是 100 / 100 = 1；一条 10 Mbps 链路的开销是 100 / 10 = 10。一条路径的总开销是该路径上所有出接口开销的总和。

rip配置 

进入全局配置模式 configure terminal 

- 启动 RIP 路由进程 router rip 

- 指定版本为 1(可选，默认可能兼容，但建议显式指定) version 1  

- 宣告直连的主类网络 RIPv1 会自动识别并汇总到主类边界 network 192.168.1.0 network 10.0.0.0

- RIP 的 15 跳限制本质上是用“牺牲收敛速度”和“牺牲路径质量”来换取“防止死循环”的简单机制



🧪 实验拓扑：不连续子网

想象一下，同一个主类网络 172.16.0.0/16 被另一个完全不同的网络 12.0.0.0/8 隔开了。

- R1 (左边)：连接子网 172.16.1.0/24

- R2 (中间)：连接 R1 和 R3，使用网络 12.1.1.0/30

- R3 (右边)：连接子网 172.16.2.0/24

冲突点：R1 和 R3 都有 172.16.x.x 的网段，但它们不直接相连。



❌ 配置方式一：开启自动汇总 (默认/冲突状态)

这是 RIPv2 的默认行为（或者 RIPv1 的唯一行为）。路由器会“自以为是”地把子网汇总成主类网络发送出去。

1. 配置命令 (以 R1 为例)

R1(config)# router rip

R1(config-router)# version 2

! 注意：这里没有输入 no auto-summary，所以自动汇总默认开启

R1(config-router)# network 172.16.0.0

R1(config-router)# network 12.0.0.0

(R3 的配置同理)

2. 冲突发生的过程

- R1 的动作：R1 看到自己直连 172.16.1.0/24，当它通过 12.1.1.0 网段（属于不同主类网络）向 R2 发送更新时，它会自动汇总，告诉 R2：“我有 172.16.0.0/16”。

- R3 的动作：R3 看到自己直连 172.16.2.0/24，同样向 R2 发送更新，告诉 R2：“我有 172.16.0.0/16”。

- R2 的困惑：R2 的路由表里出现了两条一模一样的路由条目：

- R 172.16.0.0/16 [120/1] via 12.1.1.1 (来自 R1)

- R 172.16.0.0/16 [120/1] via 12.1.1.2 (来自 R3)

3. 最终结果：路由黑洞

当 R2 收到一个要去往 172.16.1.5 (在 R1 那边) 的数据包时：

- R2 查找路由表，发现两条去往 172.16.0.0/16 的路。

- 因为开销（跳数）一样，R2 可能会负载均衡，把数据包发给 R3。

- R3 收到数据包，一看目标是 172.16.1.5，但它只知道 172.16.2.0，且没有明细路由，于是丢弃数据包。

- 现象：网络时通时断，或者完全不通。



✅ 配置方式二：关闭自动汇总 (正确/无冲突)

这是现代网络的标准配置。我们强制路由器发送“明细路由”。

1. 配置命令 (以 R1 为例)

R1(config)# router rip

R1(config-router)# version 2

R1(config-router)# no auto-summary  <-- 关键命令！关闭自动汇总

R1(config-router)# network 172.16.0.0

R1(config-router)# network 12.0.0.0

(R2 和 R3 也必须同样配置 no auto-summary)

2. 冲突解决的过程

- R1 的动作：R1 向 R2 发送更新时，不再汇总，而是明确说：“我有 172.16.1.0/24”。

- R3 的动作：R3 向 R2 发送更新时，明确说：“我有 172.16.2.0/24”。

- R2 的清晰认知：R2 的路由表变得非常清晰：

- R 172.16.1.0/24 [120/1] via 12.1.1.1 (指向 R1)

- R 172.16.2.0/24 [120/1] via 12.1.1.2 (指向 R3)

3. 最终结果：通信正常

当 R2 收到去往 172.16.1.5 的数据包时：

- R2 精确匹配到 172.16.1.0/24 这条路由。

- R2 毫不犹豫地将数据包转发给 R1。

- 现象：全网互通，没有冲突。



📊 总结对比表

所以，配置 no auto-summary 本质上就是告诉路由器：“不要自作聪明帮我合并路由，把具体的门牌号（子网）如实汇报给邻居。”

- 无4.14笔记

- 在 OSPF 协议中，你提到的“域边界路由器”通常指的是区域边界路由器（Area Border Router，简称 ABR）。

- 它是 OSPF 多区域网络架构中的核心组件，起着“桥梁”和“关口”的作用。以下我为你详细拆解它的定义、功能以及在路由表中的表现：

- 1. 核心定义：什么是 ABR？

- ABR 是连接不同 OSPF 区域的路由器。它的身份特征非常明确：

- 多接口归属：ABR 至少有两个接口，分别属于不同的 OSPF 区域。

- 必须连接骨干区域：根据 OSPF 的星型拓扑规则，ABR 必须至少有一个接口连接在骨干区域（Area 0）上。

- 双重身份：它既是骨干区域的一部分，也是非骨干区域的一部分。

- 2. ABR 的三大核心功能

- ABR 不仅仅是一个转发数据的设备，它在 OSPF 的数据库维护和路由计算中扮演着复杂的角色：

- 🌉 区域间的“桥梁”（路由转发）

- 当 Area 1 的路由器想要发送数据给 Area 2 的路由器时，数据包必须先发送给 ABR，由 ABR 转发到骨干区域，再由骨干区域的 ABR 转发到目标区域。ABR 负责在不同区域之间传递路由信息。

- 🗄️ 独立的数据库维护（LSDB）

- 这是 ABR 最显著的特征之一。ABR 会为每一个连接的区域维护一份独立的链路状态数据库（LSDB）。

- 例如，一台 ABR 连接了 Area 0 和 Area 1，它手里就有两份“地图”：一份是 Area 0 的拓扑图，一份是 Area 1 的拓扑图。

- 这使得 ABR 能够隔离拓扑变化。如果 Area 1 的链路频繁震荡，这种影响会被限制在 Area 1 的 LSDB 中，不会导致 Area 0 的路由器频繁重新计算路由。

- 📢 路由汇总与通告（LSA 生成）

- ABR 负责将区域内的路由信息“翻译”并通告给其他区域：

- 生成 Type 3 LSA（Network Summary LSA）：ABR 会把 Area 1 内部的网段信息，打包成 Type 3 LSA 发送到 Area 0；反之亦然。

- 路由聚合：为了减小路由表规模，ABR 可以在边界将连续的网段聚合成一条大路由发布。

- 生成 Type 4 LSA：如果网络中有 ASBR（自治系统边界路由器），ABR 还会生成 Type 4 LSA，告诉其他区域的路由器“如何到达 ASBR”。

- 开端口，启路由，查网段

在思科（Cisco）的 OSPF 实现中，虚链路（Virtual Link）与普通物理链路虽然都能建立邻居关系并传递路由，但它们在本质、工作机制、流量转发以及配置逻辑上有着天壤之别。

简单来说：普通链路是“修路”，虚链路是“搭桥”。

以下是详细的对比分析：

1. 核心区别对比表



2. 深度解析：四大关键差异

🛣️ 本质与归属：它是 Area 0 的“延伸”

- 普通链路：如果你把接口配在 Area 1，它就是 Area 1 的一部分。

- 虚链路：这是最容易让人晕的地方。虚链路虽然在 Area 1 中传输，但在 OSPF 的逻辑眼里，它被视为 Area 0 的一部分。

- 作用：它把远端的 ABR 逻辑上“拉”到了骨干区域，让它以为自己直接连着 Area 0。

🚚 流量转发：只传“信”，不运“货”

这是最大的误区。很多人以为配了虚链路，数据流量就会走这条“捷径”。

- 普通链路：既传路由信息（LSA），也传业务数据流量。

- 虚链路：主要用来传递**路由信息（LSA）**和建立邻居关系。

- 注意：虽然理论上数据可以通过虚链路转发（因为路由表会计算出路径），但由于它是逻辑隧道，数据包会被封装并在传输区域中再次路由，这通常会导致次优路径或增加延迟。因此，虚链路通常只用于“救急”或修复拓扑，不建议作为主要的数据传输路径。

📡 报文发送机制：组播 vs 单播

- 普通链路：在广播网络中，OSPF 使用组播地址 224.0.0.5 发送 Hello 包，大家都能听到。

- 虚链路：因为中间隔着复杂的传输区域，组播包很难穿透。所以思科的虚链路使用单播。

- 路由器会查看传输区域的 LSDB（链路状态数据库），计算出到达对端 Router ID 的路径，然后像发快递一样，把 OSPF 报文单播给对端。

⏱️ 邻居维护机制（思科特性）

- 普通链路：雷打不动地每 10秒（或 30秒）发一次 Hello 包。如果收不到，邻居就断掉。

- 思科虚链路：思科做了一种优化。一旦虚链路建立，它会将该链路视为按需电路。

- 邻居建立后，不再周期性发送 Hello 包（Hello 被抑制），邻居状态显示为 FULL 且不会老化。这节省了传输区域的带宽资源。（注：华为设备默认通常仍保留周期性 Hello）。



3. 配置上的直观区别

普通链路配置（在接口下）：

interface GigabitEthernet0/0

 ip address 10.1.1.1 255.255.255.0

!

router ospf 1

 network 10.1.1.0 0.0.0.255 area 1  <-- 宣告接口进区域

虚链路配置（在 OSPF 进程下）：

router ospf 1

 area 1 virtual-link 2.2.2.2  <-- 在传输区域(1)中，指定对端ID(2.2.2.2)

注意：虚链路不需要在接口下配置，而是在 OSPF 路由进程下直接配置。

4. 总结

你可以这样理解：

- 普通 OSPF 链路是实体道路，车（数据）在路上跑，交警（OSPF）在路口指挥。

- OSPF 虚链路是专用电话线。两个被大山（非骨干区域）隔开的指挥官（ABR），通过这根电话线直接通话（交换 LSA），确认彼此属于同一个指挥部（Area 0）。虽然理论上士兵（数据包）也可以顺着电话线爬过去，但这显然不是设计初衷。

建议：虚链路应仅作为临时修复方案（如修复不连续 Area 0）或备用方案。如果长期依赖虚链路，说明网络拓扑设计（Area 规划）存在缺陷。

OSPF 核心概念笔记

OSPF（开放式最短路径优先）协议通过三种核心元素的协同工作来实现动态路由：分组是通信的语言，表是信息的载体，工作状态是建立关系和同步信息的过程。

一、 OSPF 的五种分组类型 (The 5 Packet Types)

分组是 OSPF 路由器之间交换信息的报文。它们各司其职，共同完成邻居发现、数据库同步和路由更新。

Hello 分组 (类型 1)

- 作用：发现和维护邻居关系，充当“心跳”；在广播网络中选举 DR/BDR。

- 类比：打招呼，确认对方在线。

数据库描述分组 (DBD, 类型 2)

- 作用：在建立邻接关系初期，交换本地链路状态数据库（LSDB）的摘要（LSA 头部），就像交换“目录”。

- 类比：交换书单目录，看看对方有什么书。

链路状态请求分组 (LSR, 类型 3)

- 作用：根据收到的 DBD 摘要，向邻居请求自己所缺少或已过时的完整 LSA 信息。

- 类比：根据书单目录，列出自己想借的书。

链路状态更新分组 (LSU, 类型 4)

- 作用：携带一个或多个完整的 LSA 信息，用于响应 LSR 请求，或主动泛洪（Flooding）网络拓扑变化。

- 类比：把对方想借的书（完整的LSA）给对方。

链路状态确认分组 (LSAck, 类型 5)

- 作用：对收到的 LSU 进行确认，保证链路状态信息交换的可靠性。

- 类比：签收快递，确认收到书籍。

二、 OSPF 的三张核心表 (The 3 Core Tables)

这三张表是 OSPF 在路由器内部维护的数据结构，它们按照特定顺序生成，环环相扣。

邻居表 (Neighbor Table)

- 内容：记录所有与本机建立 OSPF 邻居关系的路由器信息（如 Router ID、状态、接口IP等）。

- 作用：管理邻居关系，是信息交换的基础。可以理解为 OSPF 的“社交圈”。

- 生成方式：通过收发 Hello 分组。

- 查看命令：display ospf peer

拓扑表 / 链路状态数据库 (LSDB)

- 内容：存储整个 OSPF 区域内所有路由器产生的 LSA（链路状态通告）的集合。

- 作用：构成一张完整的网络拓扑“地图”。同一区域内的所有路由器，其 LSDB 是完全一致的。

- 生成方式：通过交换 DBD、LSR、LSU、LSAck 分组 来同步。

- 查看命令：display ospf lsdb

路由表 (Routing Table)

- 内容：存放由 OSPF 计算出的最优路径条目（目标网络、下一跳、开销等）。

- 作用：指导数据包的转发。这是 OSPF 工作的最终“导航指令”。

- 生成方式：以 LSDB 为输入，运行 SPF（最短路径优先）算法独立计算得出。

- 查看命令：display ip routing-table protocol ospf

三、 OSPF 的工作状态 (The Neighbor State Machine)

工作状态描述了 OSPF 邻居关系从建立到同步完成的完整过程，是一个状态机。

阶段一：建立邻居关系

- Down：初始状态，尚未收到邻居的 Hello 包。

- Init：收到了邻居的 Hello 包，但双向通信尚未确认。

- 2-Way：关键状态。双向通信确认，邻居关系建立。在广播网络中，会在此状态下进行 DR/BDR 选举。

阶段二：同步链路状态数据库

- ExStart：准备交换数据库。协商主从（Master/Slave）关系，为有序交换 DBD 做准备。

- Exchange：互相交换 DBD 分组（数据库摘要/目录）。

- Loading：根据 DBD 摘要，发送 LSR 分组请求缺失信息，并接收邻居回复的 LSU 分组（完整LSA），最后用 LSAck 分组确认。

阶段三：完成

- Full：最终稳定状态。双方的 LSDB 已完全同步，邻接关系建立。路由器可以基于此运行 SPF 算法计算路由。



核心关系总结

这三者之间的关系可以概括为一个清晰的流程：

- 通过分组建立关系：路由器通过交换 Hello 分组，建立 邻居表。

- 通过状态同步信息：邻居关系沿着 工作状态机（ExStart → Exchange → Loading）前进，通过交换 DBD/LSR/LSU/LSAck 分组，最终同步彼此的 拓扑表（LSDB），达到 Full 状态。

- 基于信息计算路由：每台路由器以自己同步好的 拓扑表（LSDB） 为基础，独立运行 SPF 算法，计算出最优路径并存入 路由表。

这个流程清晰地展示了 OSPF 从“交朋友”到“对地图”再到“算路径”的完整工作逻辑。

❌ 重要澄清：Loopback 接口的子网掩码 绝非固定，更不是“必须四个255"！

这是一个非常常见的误解，我们来彻底厘清：



🔍 核心结论



🌰 实际配置示例（Cisco IOS）

! ✅ 合法：/32（最常见，代表“本路由器自身”）

interface Loopback0

 ip address 1.1.1.1 255.255.255.255



! ✅ 合法：/24（用于模拟子网或测试）

interface Loopback1

 ip address 192.168.10.1 255.255.255.0



! ✅ 合法：/16（大型网络设计场景）

interface Loopback2

 ip address 10.1.0.1 255.255.0.0



! ✅ 合法：/30（虽不推荐，但技术上可行）

interface Loopback3

 ip address 172.16.0.1 255.255.255.252

所有配置均能生效，设备不会报错！



❓ 为什么会有“必须 /32"的误解？

1️⃣ OSPF 协议的“默认行为”干扰

interface Loopback0

 ip address 10.0.0.1 255.255.255.0  ! ← 接口实际掩码是 /24

!

router ospf 1

 network 10.0.0.0 0.0.0.255 area 0

- 现象：OSPF 会将此 Loopback 宣告为 10.0.0.1/32（主机路由）

- 原因：OSPF 默认将 Loopback 视为“点对点主机”，强制以 /32 发布

- 关键区分：🔸 接口实际掩码 = 你配置的 255.255.255.0（/24）🔸 OSPF 宣告的掩码 = 协议处理后的 /32（可通过 ip ospf network point-to-point 改变）

2️⃣ 最佳实践被误读为“强制规则”

- ✅ 为什么 /32 是主流选择？

- Loopback 是逻辑接口，无物理链路，无需广播域

- 代表“路由器自身标识”，/32 最符合语义（单主机）

- 节省 IP 地址资源（尤其在大型网络）

- 避免路由协议对子网的歧义处理（如 RIP 可能忽略 /32 路由，需注意！）



⚠️ 与路由重分布的关联（呼应您之前的问题！）

在 RIP/OSPF 重分布场景中，Loopback 掩码选择直接影响路由传递：



💡 一句话总结

Loopback 接口的子网掩码 = 你配置什么就是什么“/32 是最佳实践” ≠ “系统强制固定”混淆 接口配置掩码 与 路由协议处理行为 是误解根源！

下次配置时请牢记：掩码选择取决于网络设计需求，而非设备限制。理解协议如何“看待”这个掩码（如 OSPF 的特殊处理），比纠结“固定规则”更重要！ 😊

- 边界路由器：RIP与OSPF重分布的“智能翻译官”与“安全守门员”

- 在RIP与OSPF混合网络中，边界路由器（Border Router） 不仅是物理连接点，更是实现协议互通、保障网络稳定的核心枢纽。其作用远超“简单转发”，具体体现在以下关键维度：

- 一、核心角色定位

- 表格

- 角色说明重要性

- 协议翻译官将RIP的“跳数语言”转换为OSPF的“Cost语言”，反之亦然解决协议“语言不通”问题

- 路由守门员控制哪些路由可跨域传播，防止无效/有害路由扩散保障网络安全性与稳定性

- 环路防火墙通过标记（Tag）、过滤策略阻断路由反馈循环避免网络震荡与黑洞

- OSPF中的ASBR在OSPF术语中，执行重分布的边界路由器即为自治系统边界路由器（ASBR）触发OSPF生成Type-5 LSA通告外部路由

🚀 从零开始：RIP/OSPF重分布完整配置指南（边界路由器实战版）

✨ 前提确认：单边路由器已配置好基础路由协议（RIP域内路由器已宣告网络，OSPF域内路由器已宣告网络且Router-ID稳定）。本指南专注边界路由器从“空白配置”到“重分布生效”全流程



📋 配置前必检清单（5秒）



🔧 边界路由器配置全流程（逐行可执行）

🌐 拓扑假设

[RIP域: 192.168.1.0/24] ←(G0/0)→ [R2: 边界] ←(G0/1)→ [OSPF域: 10.0.0.0/24 Area 0]

                              Loopback0: 2.2.2.2/32

✅ 步骤1：配置接口（含Loopback - 边界路由器生命线）

enable

configure terminal



! 创建Loopback（Router-ID载体 + 管理IP）

interface Loopback0

 ip address 2.2.2.2 255.255.255.255

 description OSPF_Router-ID

 exit



! RIP域连接接口

interface GigabitEthernet0/0

 ip address 192.168.1.2 255.255.255.0

 description TO_RIP_DOMAIN

 no shutdown

 exit



! OSPF域连接接口

interface GigabitEthernet0/1

 ip address 10.0.0.1 255.255.255.0

 description TO_OSPF_DOMAIN

 no shutdown

 exit

✅ 步骤2：配置OSPF基础（必须宣告Loopback）

router ospf 1

 router-id 2.2.2.2          ! 手动绑定Loopback IP（防重启漂移）

 network 10.0.0.0 0.0.0.255 area 0   ! OSPF域接口

 network 2.2.2.2 0.0.0.0 area 0      ! Loopback精确宣告（通配符0.0.0.0）

 exit

✅ 步骤3：配置RIP基础（必须关闭自动汇总）

router rip

 version 2                 ! 启用RIPv2（支持VLSM）

 network 192.168.1.0       ! RIP域接口网段（主类网络）

 no auto-summary           ! ⚠️ 关键！否则子网路由丢失

 exit

✅ 步骤4：配置重分布（二选一，推荐方案A）

🥇 方案A：单向重分布（OSPF访问RIP网络 - 安全首选）

router ospf 1

 redistribute rip subnets metric 100 metric-type 2

! 解释：

!   subnets → 传递RIP的/25、/26等子网（必加！）

!   metric 100 → 外部路由Cost（合理值50-200）

!   metric-type 2 → E2类型（默认，仅计算外部Cost）

 exit

✅ 效果：OSPF域可访问RIP网络，RIP域无法访问OSPF网络（安全隔离）

🥈 方案B：双向重分布（必须配防环！仅当业务强制要求）

! --- 创建防环策略 ---

route-map RIP_TO_OSPF permit 10

 set tag 100               ! 为RIP路由打标签



route-map FILTER_OSPF deny 10

 match tag 100             ! 拒绝带标签100的路由（防回注）

route-map FILTER_OSPF permit 20



! --- 执行重分布 ---

router ospf 1

 redistribute rip subnets route-map RIP_TO_OSPF metric 100 metric-type 2

 exit



router rip

 redistribute ospf 1 metric 2 route-map FILTER_OSPF

! ⚠️ metric 2 必须指定！值范围1-14（15=不可达）

 exit



🔍 验证三连（配置后立即执行）

! 1. 检查Router-ID是否稳定

R2# show ip ospf | include Router ID

! ✅ 正确输出：Router ID 2.2.2.2



! 2. 检查重分布路由是否学习

R2# show ip route ospf | include 192.168    ! 应看到"O E2 192.168.1.0/24"

R2# show ip route rip | include 10.0        ! （双向时）应看到"R 10.0.0.0/24"



! 3. 检查重分布策略是否生效

R2# show ip protocols | section redistribute

! ✅ 应显示：

!   Redistributing: rip

!   Redistributing: ospf 1 with metric 2



🌐 端到端连通性测试（终极验证）

! 从OSPF域设备测试RIP网络（单向/双向均应通）

R3# ping 192.168.1.10 source 10.10.0.1

! Type escape sequence to abort.

! Sending 5, 100-byte ICMP Echos to 192.168.1.10, timeout is 2 seconds:

! !!!!!  ← 全通 = 成功！



! （双向时）从RIP域设备测试OSPF网络

R1# ping 10.10.0.10

! !!!!!  ← 全通 = 双向成功



⚠️ 从零配置致命陷阱（避坑指南）



💡 终极行动口诀（背下来！）

“Loopback定ID，subnets保子网；RIP关汇总，metric必指定；单向最安全，双向tag防环！”



📌 重要提醒

- 单边路由器无需改动：它们只需保持原有RIP/OSPF基础配置（已能学习域内路由）

- 重分布是边界路由器专属任务：其他路由器绝不配置redistribute命令

- 先单向，后双向：首次配置强烈推荐方案A（单向），验证成功后再考虑双向

- 配置即生效：命令输入后路由表立即更新，无需重启

遇到问题？先执行show ip route确认基础路由是否存在 → 再查重分布配置！ 😎

一致性：同一个区域内的所有路由器都必须配置 stub，否则 OSPF 邻居关系会卡在 EXSTART/EXCHANGE 状态，无法建立邻接。

禁止 ASBR：Stub 区域内不能运行 ASBR（即不能在该区域内引入外部路由，如 RIP、BGP 等），否则该区域会自动变成非 Stub 状态或导致配置失败。

骨干区限制：Area 0 骨干末节区 完全末节区

ASBR 在外面（Area 0）：负责引入路由。

Totally Stub 在里面（Area 1）：负责“偷懒”，不看具体路由，只走默认出口。

ABR 在中间：负责把外面的复杂路由屏蔽掉，只给里面留一个出口。

铁律:

必须有骨干区域（Area 0）：所有 OSPF 网络都必须有一个 Area 0。

所有区域必须与 Area 0 相连：非骨干区域不能孤立存在。

邻居参数必须一致：比如 Hello 时间、Dead 时间、认证密码等必须一样，邻居才能建立。（区域配置一致性）

area 4: 指定要配置的区域ID为4。

nssa: 将该区域标记为NSSA。

作用：允许该区域内存在ASBR（自治系统边界路由器），可以将外部路由（如RIP、BGP等）重分发进OSPF。

LSA类型：引入的外部路由以 Type 7 LSA 的形式在该区域内传播，而不是标准的 Type 5 LSA。

no-summary: 这是一个仅在 ABR (区域边界路由器) 上有效的参数。

作用：阻止 ABR 向该区域发送 Type 3 LSA（区域间汇总LSA）。

结果：区域内的普通路由器除了知道本区域的链路状态外，不知道其他OSPF区域的具体路由。

默认路由：ABR会自动向该区域注入一条 Type 7 LSA格式的默认路由 (0.0.0.0/0)，以便区域内设备访问外部网络和其他OSPF区域。



备份（Backup）和冗余（Redundancy）是数据保护和系统高可用性的两个核心概念，虽然它们都旨在防止数据丢失或系统中断，但实现方式和侧重点有所不同。

1. 定义与区别

VRRP（虚拟路由冗余协议）是一种用于提升网络可靠性的容错协议，通过将多台路由器虚拟成一台设备，实现网关的冗余备份。当主网关设备发生故障时，VRRP能自动将流量切换到备份设备，确保网络通信不中断。引入VRRP主要是为了解决单点故障问题，保障关键业务（如IPTV、视频会议等）的连续性和稳定性。

DHCP（动态主机配置协议）

一、DHCP 概述

DHCP (Dynamic Host Configuration Protocol) 是一种应用层网络协议，基于 UDP 传输。

- 核心作用：自动为局域网内的客户端（PC、手机、服务器等）分配 IP 地址、子网掩码、默认网关、DNS 服务器等网络参数。

- 主要优势：

- 简化网络管理，无需手动配置每台设备。

- 避免 IP 地址冲突。

- 提高 IP 地址利用率（通过租期机制回收未使用的地址）。

- 端口信息：

- 服务器监听端口：UDP 67

- 客户端发送/接收端口：UDP 68



二、核心工作流程（DORA 过程）

DHCP 客户端获取 IP 地址的过程通常被称为 DORA 流程，包含四个关键步骤：

1. Discover（发现阶段）

- 触发条件：客户端启动或检测到当前没有 IP 地址时。

- 动作：客户端广播发送 DHCP Discover 报文。

- 报文特征：

- 源 IP：0.0.0.0（因为此时还没有 IP）

- 目的 IP：255.255.255.255（广播地址）

- 源 MAC：客户端的 MAC 地址

- 目的 MAC：FF:FF:FF:FF:FF:FF（广播 MAC）

- 目的：在局域网内寻找可用的 DHCP 服务器。

2. Offer（提供阶段）

- 触发条件：DHCP 服务器收到 Discover 报文后。

- 动作：服务器从地址池中选择一个可用的 IP 地址，并通过 DHCP Offer 报文回复给客户端。

- 报文特征：

- 如果网络中存在多个 DHCP 服务器，它们都会回复 Offer。

- 报文包含：建议分配的 IP 地址、租期时间、服务器标识符（Server ID）。

- 传输方式：通常是广播（因为客户端此时仍无 IP），但在某些实现中可以是单播（取决于客户端标志位）。

3. Request（请求阶段）

- 触发条件：客户端收到一个或多个 Offer 后。

- 动作：客户端选择第一个收到的 Offer（或根据策略选择），并广播发送 DHCP Request 报文。

- 报文内容：

- 明确告知所有 DHCP 服务器：“我选择了你提供的 IP”以及“拒绝其他服务器的 Offer”。

- 包含被选中的服务器标识符（Server Identifier）。

- 目的：正式请求使用该 IP 地址，并通知未被选中的服务器释放其保留的资源。

4. Acknowledgment（确认阶段 / ACK）

- 触发条件：被选中的 DHCP 服务器收到 Request 报文后。

- 动作：服务器发送 DHCP Ack 报文进行最终确认。

- 结果：

- 客户端收到 ACK 后，正式将 IP 地址绑定到网卡上，完成配置。

- 如果服务器无法分配该 IP（例如已被占用），则发送 DHCP NAK（否定确认），客户端需重新发起 DORA 流程。



三、关键补充知识点

1. 跨网段支持（DHCP 中继）

- 问题：上述 DORA 过程默认基于广播。路由器默认不转发广播包，因此不同网段的客户端无法直接联系到远程的 DHCP 服务器。

- 解决方案：使用 DHCP 中继代理 (DHCP Relay Agent)。

- 位于客户端所在网段的路由器或三层交换机上。

- 功能：捕获客户端的广播 Discover 报文，将其封装为单播报文发送给指定的远程 DHCP 服务器。

- 响应路径：服务器回应的单播报文发给中继，中继再转为广播或单播发给客户端。

2. IP 租约管理 (Lease Management)

- 租期 (Lease Time)：DHCP 分配的 IP 地址不是永久的，有一个有效期（如 8 小时、24 小时等）。

- 续租 (Renewal)：

- T1 时刻 (50%)：客户端尝试向原服务器单播发送 DHCP Request 请求续租。若成功，租期重置。

- T2 时刻 (87.5%)：如果 T1 时刻未收到回应，客户端广播 DHCP Request 寻找任何可用的 DHCP 服务器续租。

- 租期耗尽：若未续租成功，IP 地址失效，客户端必须重新进入 DORA 流程。

3. 静态绑定 (Reservation)

- 管理员可以将特定的 MAC 地址与固定的 IP 地址在 DHCP 服务器上绑定。

- 这样既享受了 DHCP 自动配置的便利，又保证了特定设备（如打印机、服务器）拥有固定 IP。



四、流程图示简述

[客户端]                  [DHCP 服务器]

   |                          |

   |-- DHCP Discover -------->|  (广播：找服务器)

   |                          |

   |<-- DHCP Offer -----------|  (广播/单播：提供IP)

   |                          |

   |-- DHCP Request ---------->|  (广播：我要这个IP)

   |                          |

   |<-- DHCP Ack -------------|  (单播/广播：确认分配)

   |                          |

   +------- IP 配置完成 ------+

五、常见应用场景

- 家庭/企业局域网：手机、电脑接入 Wi-Fi 或网线后自动上网。

- 大型数据中心：虚拟机动态创建时自动获取网络配置。

- 物联网 (IoT)：大量设备接入网络时的自动化管理。

- 公共热点：访客网络自动分配临时 IP。



注：以上内容基于标准 DHCPv4 协议整理，DHCPv6 的工作流程略有不同（使用 SLAAC 或 DHCPv6 状态模式）。

思科 (Cisco) DHCP 配置示范：多种拓扑场景

在 Cisco IOS 设备（路由器或三层交换机）上配置 DHCP 服务，主要涉及DHCP 服务器和DHCP 中继代理两种角色。以下提供三种常见拓扑场景的配置示例。



场景一：单网段本地 DHCP 服务器

适用场景：小型办公室或实验室，所有客户端与 DHCP 服务器在同一广播域（同一 VLAN/子网）。 特点：无需中继，直接通过广播交互。

拓扑描述

- 设备：Router1 (作为 DHCP 服务器), Switch, PC1 (客户端)

- 网络：VLAN 10 (192.168.10.0/24)

配置步骤 (Router1)

! 1. 进入全局配置模式

enable

configure terminal



! 2. 关闭域名查询 (可选，防止 ping 错误命令时尝试 DNS 解析)

no ip domain-lookup



! 3. 创建 DHCP 地址池 (Pool Name)

ip dhcp pool LAN_POOL_10

 network 192.168.10.0 255.255.255.0      ! 定义网段

 default-router 192.168.10.254           ! 默认网关

 dns-server 8.8.8.8 114.114.114.114      ! DNS 服务器

 lease 7 0                               ! 租期：7天 0小时 (可省略，默认为1天)

 exit



! 4. 排除不分配给客户端的 IP (如网关、服务器IP等)

! 注意：必须在创建 pool 之前或之后配置，但通常建议在 pool 外配置

ip dhcp excluded-address 192.168.10.1 192.168.10.10    ! 保留前10个IP

ip dhcp excluded-address 192.168.10.254                 ! 保留网关IP



! 5. 配置接口并开启 DHCP 服务

interface GigabitEthernet0/0

 description Link_to_Switch

 ip address 192.168.10.254 255.255.255.0

 no shutdown

exit



! 6. 验证配置

show ip dhcp pool

show ip dhcp binding

clear ip dhcp binding *   ! 清除现有绑定进行测试



场景二：多网段 DHCP 中继 (DHCP Relay)

适用场景：企业园区网，多个 VLAN 分布在不同的二层交换机上，DHCP 服务器集中在一台核心路由器或服务器上。 特点：利用 ip helper-address 将广播请求转换为单播转发到指定服务器。

拓扑描述

- DHCP 服务器: Router_Server (IP: 10.0.0.1)

- 中间设备: Router_A (连接 VLAN 10, VLAN 20)

- 客户端: PC1 (VLAN 10), PC2 (VLAN 20)

- VLAN 10: 192.168.10.0/24 (网关 192.168.10.254)

- VLAN 20: 192.168.20.0/24 (网关 192.168.20.254)

配置步骤

A. 配置 DHCP 服务器端 (Router_Server)

注意：服务器需要知道如何路由回各个子网，或者使用 ip helper-address 指向服务器即可，服务器需配置对应子网的 Pool。

enable

configure terminal



! 配置服务器自身的接口 (假设直连或路由可达)

interface GigabitEthernet0/1

 ip address 10.0.0.1 255.255.255.0

 no shutdown

exit



! 确保有回程路由到达 192.168.x.x 网段 (静态路由或动态路由)

ip route 192.168.0.0 255.255.0.0 10.0.0.254 



! 创建两个地址池

ip dhcp pool VLAN10_POOL

 network 192.168.10.0 255.255.255.0

 default-router 192.168.10.254

 dns-server 8.8.8.8

 exit



ip dhcp pool VLAN20_POOL

 network 192.168.20.0 255.255.255.0

 default-router 192.168.20.254

 dns-server 8.8.8.8

 exit



! 排除地址

ip dhcp excluded-address 192.168.10.1 192.168.10.254

ip dhcp excluded-address 192.168.20.1 192.168.20.254

B. 配置中间路由器 (Router_A) - 开启中继

这是关键步骤：在每个 VLAN 的 SVI 接口下配置 ip helper-address。

enable

configure terminal



! 配置 VLAN 10 接口

interface Vlan10

 ip address 192.168.10.254 255.255.255.0

 ! 指向 DHCP 服务器的 IP

 ip helper-address 10.0.0.1 

 no shutdown

exit



! 配置 VLAN 20 接口

interface Vlan20

 ip address 192.168.20.254 255.255.255.0

 ! 指向同一个 DHCP 服务器

 ip helper-address 10.0.0.1

 no shutdown

exit



! 确保路由器之间路由互通

ip route 10.0.0.0 255.255.255.0 10.0.0.254 (假设上游路由)

原理解析：当 PC1 发送广播 Discover 报文时，Router_A 收到后，将其封装为单播报文发送给 10.0.0.1 (Server)。Server 回复 Offer 给 Router_A，Router_A 再根据 Client 的 MAC 地址转发给 PC1。



场景三：特殊选项配置 (DHCP Option 150/66)

适用场景：为 Cisco IP 电话、无线 AP 或摄像头自动下发 TFTP 服务器地址（用于加载配置文件）。 原理：在 DHCP Pool 中配置 option 参数。

拓扑描述

- 设备: Router_DHCP

- 需求: 为 IP 电话分配 IP 的同时，告知其 TFTP 服务器地址 (例如 192.168.100.5)。

配置步骤

enable

configure terminal



! 创建专用地址池 (例如用于 VoIP 终端)

ip dhcp pool VOIP_POOL

 network 192.168.100.0 255.255.255.0

 default-router 192.168.100.254

 dns-server 8.8.8.8



 ! 配置 Option 150 (Cisco 私有标准，推荐)

 ! 格式：option 150 ip <TFTP_Server_IP>

 option 150 ip 192.168.100.5



 ! 或者配置 Option 66 (通用 TFTP Server Hostname/IP)

 ! option 66 ip 192.168.100.5



 ! 排除地址

 ip dhcp excluded-address 192.168.100.1 192.168.100.254

 exit



! 如果需要在特定接口启用该 Pool，可以使用 match-client 或基于 MAC 地址分配

! 但通常只要 IP 地址落在该 Pool 范围内即可自动匹配。



常用验证与排错命令

常见排错思路

- 客户端收不到 IP：

- 检查 ip helper-address 是否配置正确（IP 地址是否正确，接口是否 UP）。

- 检查服务器端是否有对应的 network 语句。

- 检查中间是否有 ACL 阻止了 UDP 67/68 端口。

- IP 冲突：

- 检查 excluded-address 范围是否包含了正在被静态使用的 IP。

- 无法获取 DNS/TFTP：

- 检查 Pool 中是否配置了 dns-server 或 option 150。



总结

- 单网段：直接在接口配 IP，配置一个 Pool 即可。

- 多网段：必须在每个 VLAN 接口配置 ip helper-address <Server_IP>，服务器需配置多个 Pool 覆盖不同网段。

- 特殊应用：利用 option 关键字扩展 DHCP 功能（如 Option 150 给电话配 TFTP）。

这些配置是构建现代企业网络的基础，熟练掌握 DORA 流程配合 Cisco 命令行的逻辑至关重要。

在思科（Cisco）设备上配置 DHCP 中继代理（DHCP Relay Agent），主要目的是让位于不同子网的 DHCP 客户端能够向位于远程子网的 DHCP 服务器请求 IP 地址。

核心配置命令是在连接客户端的接口下使用 ip helper-address。

以下是基于 Cisco IOS（路由器或三层交换机）的标准配置示范：

场景假设

- DHCP 服务器 IP: 192.168.10.5 (位于管理网段)

- 客户端网段 1: 192.168.20.0/24 (连接在接口 GigabitEthernet0/1)

- 客户端网段 2: 192.168.30.0/24 (连接在接口 GigabitEthernet0/2)

- 中继设备: Cisco Router/Switch



配置步骤与代码

! 进入全局配置模式

Router> enable

Router# configure terminal



! --- 配置连接客户端的接口 1 (VLAN 20) ---

Router(config)# interface GigabitEthernet0/1

Router(config-if)# description Connection to VLAN 20 Clients

Router(config-if)# ip address 192.168.20.1 255.255.255.0

Router(config-if)# no shutdown

! 关键步骤：指定 DHCP 服务器的 IP 地址

! 该命令会将接收到的 UDP 广播包（端口 67/68）转换为单播包，转发给指定的服务器

Router(config-if)# ip helper-address 192.168.10.5

Router(config-if)# exit



! --- 配置连接客户端的接口 2 (VLAN 30) ---

Router(config)# interface GigabitEthernet0/2

Router(config-if)# description Connection to VLAN 30 Clients

Router(config-if)# ip address 192.168.30.1 255.255.255.0

Router(config-if)# no shutdown

! 同样指向同一个 DHCP 服务器

Router(config-if)# ip helper-address 192.168.10.5

Router(config-if)# exit



! --- (可选) 配置默认路由，确保路由器能回包给客户端 ---

! 如果 DHCP 服务器不在直连网段，需要确保有路由可达

Router(config)# ip route 0.0.0.0 0.0.0.0 <下一跳IP或出口接口>



! 保存配置

Router(config)# end

Router# write memory



关键点解析



ip helper-address <server-ip>:



- 这是实现 DHCP 中继的核心命令。

- 它通常配置在接收 DHCP Discover 广播包的接口上（即连接客户端的接口）。

- 默认情况下，该命令会转发以下 UDP 端口：

- 67: DHCP Server

- 68: DHCP Client

- 69: TFTP

- 137, 138: NetBIOS

- 139, 49: NetBIOS / DNS

- 42: Name Service

- 53: DNS

- 19: Character Generator

- 如果你只想转发 DHCP 流量而不影响其他服务，或者需要转发自定义端口，可以使用 ip forward-protocol udp <port> 进行精细控制（但在大多数网络中，默认行为已足够且推荐）。



Option 82 (Relay Agent Information Option):



- 在某些安全要求较高的网络中，可能需要启用 Option 82 来标识客户端的物理位置（如接入交换机和端口）。

- 配置示例（在全局模式下）：

Router(config)# ip dhcp relay information option

! 或者针对特定接口丢弃不信任的 Option 82 信息

Router(config)# interface GigabitEthernet0/1

Router(config-if)# ip dhcp relay information trusted



防火墙/ACL 注意事项:



- 确保中间路径上的任何防火墙或 ACL 允许 UDP 67 和 UDP 68 端口的通信。

- 注意方向：

- 客户端 -> 中继：UDP 68 (源) -> UDP 67 (目的) [广播]

- 中继 -> 服务器：UDP 67 (源) -> UDP 67 (目的) [单播，源IP变为中继接口IP]

- 服务器 -> 中继：UDP 67 (源) -> UDP 68 (目的) [单播]

- 中继 -> 客户端：UDP 67 (源) -> UDP 68 (目的) [广播或单播]

验证命令

配置完成后，可以使用以下命令检查状态：

! 查看接口的中继配置

Router# show running-config interface GigabitEthernet0/1



! 查看 DHCP 绑定表（如果路由器也充当了部分角色或记录了转发表）

Router# show ip dhcp binding



! 调试 DHCP 中继过程（在生产环境慎用，会产生大量日志）

Router# debug ip dhcp server packet

Router# debug ip udp

如果是 Cisco ASA (防火墙)

如果在 Cisco ASA 防火墙上配置，命令略有不同：

ASA(config)# interface inside

ASA(config-if)# nameif inside

ASA(config-if)# security-level 100

ASA(config-if)# ip address 192.168.20.1 255.255.255.0

ASA(config-if)# dhcpd relay 192.168.10.5 inside

! 注意：ASA 通常需要在 DHCP 服务器所在的接口配置 dhcpd server 或使用 relay 指向外部

注：ASA 的配置通常更复杂，涉及 dhcpd 进程和具体的接口映射。对于标准路由器/三层交换机，上述 ip helper-address 是最通用的方案。

<br>

ftp 远程登录/ip/域名 文件传输协议

电子邮件服务器 smtp 简单邮件传输协议pop3 邮局传输协议 

SMTP（简单邮件传输协议）和 POP3（邮局协议第3版）是电子邮件系统中两个最核心的协议，它们分别负责邮件的发送和接收。

以下是它们的详细对比和原理解析：

1. SMTP (Simple Mail Transfer Protocol)

- 中文名称：简单邮件传输协议

- 主要功能：发送邮件和服务器之间的转发。

- 工作原理：

- 当你点击“发送”按钮时，你的邮件客户端（如 Outlook, Foxmail, 手机邮件App）会使用 SMTP 协议将邮件推送到发件人的邮件服务器。

- 发件人服务器也会使用 SMTP 协议将邮件转发给收件人的邮件服务器。

- 它只负责“投递”，不负责从服务器上把邮件取下来阅读。

- 常用端口：

- 25：传统端口，常用于服务器之间传输，但常被ISP封锁。

- 587：推荐用于客户端提交邮件（需加密/STARTTLS）。

- 465：用于 SMTPS（SSL加密连接），旧标准但广泛支持。

- 特点：基于 TCP，无状态（不记录邮件是否被读取），通常不需要身份验证即可在受信任的服务器间传输，但现代服务要求客户端提交时必须验证。



2. POP3 (Post Office Protocol version 3)

- 中文名称：邮局协议第3版

- 主要功能：接收邮件（下载）。

- 工作原理：

- 当你打开邮件客户端查看新邮件时，POP3 协议会将邮件服务器上的邮件下载到你的本地设备（电脑或手机）。

- 关键特性：默认情况下，POP3 在下载邮件后会从服务器上删除该邮件（虽然现在的客户端通常可以设置“在服务器上保留副本”，但这只是模拟行为，本质上还是下载逻辑）。

- 这意味着邮件主要存储在本地，如果你换了一台设备登录，可能看不到之前在其他设备上下载的邮件。

- 常用端口：

- 110：普通明文连接。

- 995：POP3 over SSL/TLS（加密连接，推荐）。

- 特点：轻量级，适合只需要在一台设备上管理邮件的用户。不支持多设备同步状态（如已读/未读标记在不同设备间不同步）。



3. 核心区别对比表

4. 补充说明：为什么现在更推荐 IMAP？

虽然你问的是 POP3，但在实际应用中，IMAP (Internet Message Access Protocol) 已经逐渐取代了 POP3 成为主流的接收协议。

- POP3：像去邮局取信，信拿回家后就从邮局撤走了（除非特意设置保留）。

- IMAP：像在邮局里看信，信还在邮局服务器上。你在手机上看了，电脑上也能看到“已读”状态；删了邮件，所有设备都会删除。

总结流程示例：

- 你写好邮件 -> 使用 SMTP 发送到你的邮箱服务器。

- 你的邮箱服务器 -> 使用 SMTP 转发到对方的邮箱服务器。

- 对方收到通知后 -> 使用 POP3 (或 IMAP) 将邮件下载到他的手机/电脑上阅读。

如果你需要配置邮件客户端，通常需要填写两组信息：

- 发件服务器 (SMTP)：地址 + 端口 (如 smtp.example.com:587)

- 收件服务器 (POP3/IMAP)：地址 + 端口 (如 pop.example.com:995 或 imap.example.com:993)



ACL（Access Control List，访问控制列表）是网络设备（如路由器、交换机、防火墙）中用于控制数据流进出的核心安全机制。它就像网络的“守门人”，根据预设的规则决定哪些数据包可以放行，哪些必须被丢弃。

以下是 ACL 的工作原理、规则类型及配置详解：



一、ACL 工作原理

ACL 的工作过程是一个逐条匹配、一旦命中即停止的过程。

- 数据包进入：当数据包到达应用了 ACL 的接口时，设备会提取数据包的关键信息（如源IP、目的IP、协议类型、端口号等）。

- 顺序匹配：设备按照 ACL 中规则的**配置顺序（从上到下）**依次检查每一条规则。

- 执行动作：

- 匹配成功：如果数据包满足某条规则的匹配条件，立即执行该规则定义的动作（permit 允许或 deny 拒绝），不再检查后续规则。

- 匹配失败：如果数据包不满足当前规则，则继续检查下一条规则。

- 隐含拒绝（Implicit Deny）：这是 ACL 最重要的原则之一。如果数据包遍历了所有规则仍未匹配任何一条，默认会被拒绝（Drop）。因此，在配置 ACL 时，通常需要在最后显式添加一条 permit any 以允许其他流量通过（除非你确实希望阻断所有未明确允许的流量）。

关键点：ACL 规则的顺序至关重要。例如，如果你先写了 deny any，那么后面的所有 permit 规则都将永远不会生效。



二、ACL 的主要类型

根据匹配条件的精细程度，ACL 主要分为两大类：

1. 标准 ACL (Standard ACL)

- 匹配依据：仅基于源 IP 地址。

- 特点：简单、高效，但控制粒度较粗。

- 编号范围：通常为 1-99 或 1300-1999。

- 适用场景：

- 只想限制某个特定网段访问整个网络。

- 不需要区分具体服务（如HTTP、FTP）。

- 缺点：因为只看源IP，如果禁止了源IP A，那么A访问网络上的所有资源（Web、邮件、数据库）都会被禁止，无法实现“只禁止访问Web”这种精细控制。

2. 扩展 ACL (Extended ACL)

- 匹配依据：基于源IP、目的IP、协议类型（TCP/UDP/ICMP等）、源端口、目的端口等多维度。

- 特点：控制粒度极细，功能强大。

- 编号范围：通常为 100-199 或 2000-2699。

- 适用场景：

- 只允许特定主机访问特定的服务器端口（如：只允许HR部门访问财务服务器的80端口）。

- 阻止特定类型的攻击流量。

- 最佳实践：扩展ACL通常应配置在靠近源端的接口上，以便尽早丢弃非法流量，节省网络带宽和设备资源。



三、ACL 规则结构示例

一个典型的 ACL 规则由三个部分组成：操作符 (Action) + 匹配条件 (Match Criteria)。

1. 标准 ACL 示例

# Cisco IOS 语法示例

access-list 10 deny host 192.168.1.50      # 拒绝来自 192.168.1.50 的所有流量

access-list 10 permit any                  # 允许其他所有流量

解释：只有 192.168.1.50 被拒绝，其他所有IP都能通过。

2. 扩展 ACL 示例

# Cisco IOS 语法示例

access-list 101 deny tcp 192.168.1.0 0.0.0.255 any eq 23  # 拒绝 192.168.1.0/24 网段使用 Telnet (端口23) 访问任何地方

access-list 101 permit tcp 192.168.1.0 0.0.0.255 any eq 80 # 允许 192.168.1.0/24 网段使用 HTTP (端口80) 访问任何地方

access-list 101 permit ip any any                        # 允许其他所有IP协议流量

解释：

- 第一行：禁止 192.168.1.x 网段进行 Telnet 远程登录。

- 第二行：允许该网段浏览网页。

- 第三行：允许其他所有流量（防止隐含拒绝导致断网）。



四、关键概念与配置原则



通配符掩码 (Wildcard Mask)



- 在ACL中，不像子网掩码那样表示“网络位”，通配符掩码表示“忽略位”。

- 0 表示必须精确匹配。

- 1 表示忽略（任意值）。

- 示例：

- host 192.168.1.1 等同于 192.168.1.1 0.0.0.0 (只匹配这一个IP)。

- 192.168.1.0 0.0.0.255 等同于 192.168.1.0/24 (匹配整个C类网段)。



方向性 (Direction)



- ACL 必须应用在接口的特定方向：

- Inbound (入站)：数据包进入接口后，路由转发前进行检查。

- Outbound (出站)：数据包从接口发出前进行检查。

- 注意：标准ACL建议应用在靠近目的地的接口（出方向），扩展ACL建议应用在靠近源地的接口（入方向）。



命名 ACL (Named ACL)



- 现代设备支持使用名称代替数字编号，便于管理和编辑（Cisco IOS 11.2+）。

- 示例：ip access-list extended WEB-FILTER ... exit



配置三条黄金法则



- 顺序第一：最具体的规则放在最前面。

- 隐含拒绝：永远不要忘记最后加一条 permit any（除非你想做完全隔离）。

- 少而精：不要创建过多的冗余规则，定期清理无效规则。

五、总结对比表

在思科（Cisco）设备中，实现你提到的“时间ACL”和“专家ACL”分别对应着 基于时间的访问控制列表（Time-based ACL） 和 基于身份/应用的下一代防火墙策略（通常结合 Cisco ISE 或 Zone-Based Firewall 实现）。

下面为你详细解析这两类高级ACL在思科设备上的具体配置方法。

一、 时间ACL (Time-based ACL)

思科的时间ACL允许你将特定的时间段（Time-range）绑定到标准或扩展ACL规则中，实现动态的流量控制。

1. 定义时间范围 (Time-range)

首先，需要在全局配置模式下定义一个时间范围。思科支持 periodic（周期性）和 absolute（绝对时间）两种格式。

! 进入全局配置模式

configure terminal



! 定义一个名为 WORK-HOURS 的时间范围

time-range WORK-HOURS

 ! 周期性生效：周一至周五的 09:00 到 18:00

 periodic weekdays 09:00 to 18:00

 exit



! （可选）定义一个绝对时间范围，例如特定日期的维护窗口

time-range MAINTENANCE-WINDOW

 absolute start 00:00 1 Jan 2026 end 23:59 31 Jan 2026

 exit

2. 创建并绑定时间ACL

在创建扩展ACL时，在规则末尾加上 time-range 关键字并指定刚才定义的名称。

! 创建扩展ACL（推荐使用命名ACL，更易维护）

ip access-list extended TIME-BASED-POLICY



 ! 在工作日的9点到18点之间，禁止 192.168.1.0/24 网段访问 SSH (端口22)

 deny tcp 192.168.1.0 0.0.0.255 any eq 22 time-range WORK-HOURS



 ! 允许其他所有 IP 流量（注意：ACL末尾默认隐含拒绝所有，所以通常需要显式允许其他流量）

 permit ip any any

 exit

3. 应用到接口

将配置好的ACL应用到具体的物理接口或VLAN接口上。

interface GigabitEthernet0/1

 ! 将ACL应用到接口的入方向（in）

 ip access-group TIME-BASED-POLICY in

 exit



! 保存配置

write memory



二、 专家ACL (基于用户身份与应用的高级控制)

思科的传统IOS路由器/交换机本身无法直接通过简单的 access-list 命令识别“抖音”、“微信”或“张三”这样的用户身份。要实现这种“专家级”控制，通常有以下两种主流方案：

方案A：结合 Cisco ISE (身份服务引擎) 实现基于用户的ACL

这是企业级最常用的方案。通过 802.1X 或 Web认证，让设备识别“是谁”在上网，并下发动态ACL（dACL）。

工作原理：

- 用户在终端接入网络，进行 802.1X 认证。

- 交换机将认证信息转发给 Cisco ISE 服务器。

- ISE 根据用户的身份（如“财务部”、“访客”），动态下发一个预定义的 dACL 到交换机接口上。

交换机侧的基础配置（开启AAA和802.1X）：

! 配置AAA，指向ISE服务器

aaa new-model

aaa authentication dot1x default group radius

aaa authorization network default group radius



! 配置RADIUS服务器（即ISE）

radius server ISE-SERVER

 address ipv4 10.1.1.100 auth-port 1812 acct-port 1813

 key MySecretKey123



! 在接口上开启认证

interface GigabitEthernet1/0/1

 authentication port-control auto

 dot1x pae authenticator

 exit

注：具体的“允许财务访问财务系统”等策略是在 Cisco ISE 的图形化界面上配置的，而不是在交换机命令行里逐条敲入。

方案B：使用 Cisco Firepower (FTD) 或 ISR 路由器的 NBAR2 实现基于应用的ACL

如果你需要识别“禁止P2P下载”、“禁止访问社交媒体”等应用层特征，需要使用具备深度包检测（DPI）功能的设备，如 Cisco Firepower 防火墙或集成了 Security 功能的路由器（使用 NBAR2 技术）。

在支持 NBAR2 的思科路由器上配置基于应用的策略：

! 1. 定义一个类映射（Class-map），匹配特定的应用协议

class-map match-any BLOCK-SOCIAL-MEDIA

 match protocol facebook

 match protocol twitter

 exit



! 2. 定义一个策略映射（Policy-map），对匹配到的应用执行动作（如丢弃）

policy-map APP-CONTROL-POLICY

 class BLOCK-SOCIAL-MEDIA

  drop

 class class-default

  permit

 exit



! 3. 将策略应用到接口上（基于接口的流量整形/策略路由）

interface GigabitEthernet0/1

 service-policy input APP-CONTROL-POLICY

 exit

💡 配置建议与注意事项

- 时间同步至关重要：时间ACL的生效完全依赖设备的系统时间。务必配置 NTP 服务器，确保设备时间准确，否则策略可能会在不该生效的时候生效。

ntp server 10.1.1.10

clock timezone Beijing 8  ! 设置时区为东八区



- 规则顺序：思科ACL严格遵循“从上到下，匹配即停止”的原则。请将限制最严格、最具体的规则（如特定时间禁止特定IP）放在最前面。

- 隐式拒绝：思科的标准和扩展ACL末尾都有一条看不见的 deny ip any any。如果你没有在最后配置 permit ip any any，除了你明确允许的流量外，其他所有流量都会被阻断。



三、总结与对比

四、如何选择？

- 简单网络隔离 -> 用 标准/扩展ACL。

- 需要分时段控制 (如上课禁用游戏) -> 用 时间ACL。

- 需要按人/部门管理 或 识别具体APP (如禁止微信但允许QQ) -> 必须使用 专家ACL (通常需要下一代防火墙或具备NAC功能的设备)。

- 端口安全

Switch> enable Switch# configure terminal Switch(config)# interface GigabitEthernet0/1 !

Switch(config-if)# switchport mode access



- Switch(config-if)# switchport port-security

- Switch(config-if)# switchport port-security mac-address sticky

- NAT的工作过程就是地址代换

- 分为静态NAT（单对单）和动态NAT（多对多）

在思科（Cisco）设备上配置 NAT（网络地址转换） 是解决 IPv4 地址短缺和实现内网安全访问互联网的核心技能。

以下是思科路由器上 NAT 配置的核心步骤、常用命令及典型场景。



一、配置前的准备：定义内部/外部接口

在思科路由器上，必须先明确哪个接口连接内网（Inside），哪个接口连接外网（Outside）。

Router(config)# interface GigabitEthernet0/0

Router(config-if)# ip nat inside   ! 标记为内部接口（连接内网）

Router(config-if)# exit



Router(config)# interface Serial0/0/0

Router(config-if)# ip nat outside  ! 标记为外部接口（连接外网/ISP）

Router(config-if)# exit



二、三种常见的 NAT 配置模式

1. 动态 PAT (端口地址转换) —— 最常用

场景：家庭或中小企业，拥有一个公网 IP，但内部有多台设备需要上网。这是解决“多对一”问题的标准方案。

步骤：



定义允许转换的内网网段（ACL）：



Router(config)# access-list 1 permit 192.168.1.0 0.0.0.255



(注：1 是 ACL 编号，192.168.1.0 0.0.0.255 表示允许该网段的所有IP)



配置 NAT 过载（Overload/PAT）：



Router(config)# ip nat inside source list 1 interface GigabitEthernet0/0 overload



- list 1: 引用上面的 ACL。

- interface GigabitEthernet0/0: 使用指定接口的 IP 作为公网出口 IP（如果接口IP是动态获取的，用此方式最方便）。

- overload: 关键参数，开启端口复用（PAT），允许多个内网IP共用这一个公网IP。

替代写法：如果你有一个固定的公网 IP（例如 203.0.113.1）：ip nat inside source list 1 203.0.113.1 overload

2. 静态 NAT (Static NAT)

场景：需要在互联网上对外提供服务的服务器（如 Web、FTP、邮件服务器），需要一对一固定映射。

配置：

! 将内网服务器的私有 IP (192.168.1.100) 永久映射到公网 IP (203.0.113.5)

Router(config)# ip nat inside source static 192.168.1.100 203.0.113.5

- 注意：不需要 ACL，也不需要 overload。外部用户访问 203.0.113.5 时，流量会自动转发给 192.168.1.100。

3. 动态 NAT (Dynamic NAT)

场景：拥有多个公网 IP，但不想全部用完，或者希望内网用户轮流使用这些公网 IP（不通过端口区分，直接换 IP）。

步骤：



创建公网 IP 地址池：



Router(config)# ip nat pool MY_POOL 203.0.113.10 203.0.113.20 netmask 255.255.255.0



(定义从 .10 到 .20 的地址池)



绑定 ACL 和地址池：



Router(config)# ip nat inside source list 1 pool MY_POOL



(注意：这里没有写 overload，表示是一对一的动态转换，IP 用完即止)



三、验证与排错命令

配置完成后，必须检查 NAT 表项是否生效。

示例输出解读 (show ip nat translations)：

Pro  Inside global     Inside local      Outside local     Outside global

tcp  203.0.113.1:1024  192.168.1.5:1024  8.8.8.8:53        8.8.8.8:53

- 这表示内网 192.168.1.5 的某个进程正在访问 Google DNS (8.8.8.8)，其源地址已被转换为公网 IP 203.0.113.1。



四、常见故障排查点

- 接口标记错误：忘记在接口下输入 ip nat inside 或 outside，导致转换不生效。

- ACL 匹配错误：定义的 ACL 范围不对，或者方向反了（ACL 只定义源地址）。

- 路由问题：内网设备没有默认网关指向路由器，或者路由器没有回程路由指向内网（通常直连路由自动存在，但需检查）。

- 防火墙干扰：如果路由器上启用了 ACL 或 Zone-Based Firewall (ZBF)，可能会拦截 NAT 后的流量。

- 重叠网络：如果内网 IP 与外网 IP 冲突（较少见），需要使用特殊的 NAT 策略。

五、完整配置示例 (PAT 场景)

假设拓扑：

- 内网：192.168.10.0/24

- 外网接口 IP：203.0.113.1

- 目标：让内网所有电脑通过这个公网 IP 上网。

enable

configure terminal



! 1. 定义内网网段

access-list 10 permit 192.168.10.0 0.0.0.255



! 2. 配置内部接口

interface GigabitEthernet0/1

 ip address 192.168.10.1 255.255.255.0

 ip nat inside

 no shutdown

exit



! 3. 配置外部接口

interface GigabitEthernet0/0

 ip address 203.0.113.1 255.255.255.0

 ip nat outside

 no shutdown

exit



! 4. 开启 NAT 过载 (PAT)

ip nat inside source list 10 interface GigabitEthernet0/0 overload